\chapter{Experiments}
\label{chap:experiments}
This chapter describes the experimental evaluation of the systems developed in previous chapters. First, the shared experiment settings are described in Section~\ref{sec:exp_settings} which includes the custom dataset split, dataset statistics, and hardware configuration. Then, experiments are presented for the user-side recommendation system (Section~\ref{sec:exp_user}), the recruiter-side recommendation system (Section~\ref{sec:exp_recruiter}), and the congestion balancing strategies (Section~\ref{sec:exp_congestion}).

\section{Experiment Settings}
\label{sec:exp_settings}

\subsection{Dataset}
\label{sec:exp_dataset}
All experiments are conducted on the CareerBuilder Job Recommendation dataset~\cite{careerbuilderdataset}, consisting of real-world job applications, user profiles and job postings from CareerBuilder.com over a 13-week period. The dataset is organised into seven consecutive 13-day temporal windows where users apply to jobs which are active in the same window. Table~\ref{tab:dataset_stats} summarises the scale of the full dataset.

\begin{table}[htbp]
    \centering
    \caption{CareerBuilder dataset statistics.}
    \label{tab:dataset_stats}
    \begin{tabular}{lr}
        \toprule
        \textbf{Statistic} & \textbf{Value} \\
        \midrule
        Total unique users       & 389,708 \\
        Total unique jobs        & 1,091,923 \\
        Total applications       & 1,603,111 \\
        Interaction matrix sparsity & 99.9996\% \\
        \bottomrule
    \end{tabular}
\end{table}

The extremely high sparsity — fewer than one in 200,000 user-job pairs has a recorded interaction — means thatcollaborative approaches relying on interaction density are infeasible. The two-tower architecture instead uses interactions as a training signal while generalising through user and job profile features, making it robust to sparse data.

\subsection{Custom Train/Validation/Test Split}
\label{sec:custom_split}

\begin{figure}[ht]
    \centering
    \begin{tikzpicture}[
        window/.style={draw, minimum width=1.4cm, minimum height=0.8cm, font=\footnotesize, fill=white},
        label_node/.style={font=\small\bfseries, align=center}
    ]
    \node [window] (w1) at (0,0)   {W1};
    \node [window] (w2) at (1.5,0) {W2};
    \node [window] (w3) at (3,0)   {W3};
    \node [window] (w4) at (4.5,0) {W4};
    \node [window] (w5) at (6.5,0) {W5};
    \node [window] (w6) at (8,0)   {W6};
    \node [window] (w7) at (9.5,0) {W7};

    \draw [dashed, line width=1.5pt, red!80!black] (5.5, 1.2) -- (5.5, -2.5)
        node[below, font=\footnotesize\itshape, text=red!80!black] {Temporal Boundary};
    \draw [-{Stealth}, thick] (-1, -0.7) -- (10.5, -0.7) node[right] {Time};

    \draw [decorate, decoration={brace, amplitude=10pt}]
        (-0.7, 0.6) -- (5.2, 0.6)
        node [midway, yshift=0.6cm, label_node, color=blue!70!black] {Training Set (Windows 1--4)};
    \node [align=center, font=\scriptsize, color=gray!80!black] at (2.25, 1.6) {Historical Data};

    \draw [decorate, decoration={brace, amplitude=10pt}]
        (5.8, 0.6) -- (10.2, 0.6)
        node [midway, yshift=0.6cm, label_node, color=green!50!black] {Validation/Test Set (Windows 5--7)};
    \node [align=center, font=\scriptsize, color=gray!80!black] at (8, 1.6) {Cold-Start Evaluation};

    \node [draw, rounded corners, fill=gray!5, font=\tiny, align=left] at (2.25, -1.8) {
        \textbullet\ All users/jobs included \\
        \textbullet\ Historical behaviour used for training
    };
    \node [draw, rounded corners, fill=gray!5, font=\tiny, align=left] at (8, -1.8) {
        \textbullet\ No overlap with training users \\
        \textbullet\ Simulates realistic deployment
    };
    \end{tikzpicture}
    \caption{Custom temporal split for the CareerBuilder dataset. Unlike the original within-window split, this approach divides by window boundary, ensuring a strict cold-start evaluation scenario.}
    \label{fig:custom_temporal_split}
\end{figure}

Rather than using the original within-window train/test labels provided by the dataset, a temporal split across windows is applied (Figure~\ref{fig:custom_temporal_split}). Windows 1--4 form the training set and Windows 5--7 form the validation and test set, with Windows 5--6 used for validation and Window 7 reserved for final evaluation.

Users marked \texttt{Split=Test} are excluded entirely (22,838 users). These users made five or more applications during the withheld 4-day test period of the original competition. Of these, 43\% have no recorded interactions in the training period whatsoever, as their only observable activity falls within the withheld test period. Including them would introduce users with structurally missing interaction records into the evaluation set, so they are excluded entirely.

This design has three consequences. First, there is a strict temporal boundary: the model never sees any interaction from the evaluation period during training. Second, the evaluation is a genuine cold-start scenario — no user appears in both the training and evaluation sets. Third, using all active users from each window group (rather than filtering by activity thresholds) avoids biasing the evaluation towards highly-engaged users. Table~\ref{tab:split_stats} shows the resulting split sizes.

\begin{table}[htbp]
    \centering
    \caption{Dataset split statistics after applying the custom temporal split.}
    \label{tab:split_stats}
    \begin{tabular}{lrrr}
        \toprule
        \textbf{Split} & \textbf{Users} & \textbf{Jobs} & \textbf{Applications} \\
        \midrule
        Training   (W1--W4) & 189,657 & 223,600 & 881,249  \\
        Validation (W5--W6) &  78,428 &  86,196 & 367,038  \\
        Test       (W7)     &  39,948 &  39,946 & 169,227  \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{Hardware and Software Configuration}
\label{sec:hardware}

All experiments are run on the Artificial Intelligence and Data Analytics Research Group (AIDA) high-performance computing cluster via SSH. Training is performed on a single NVIDIA A40 GPU (46~GB VRAM). The software stack uses Python with PyTorch as the deep learning framework, scikit-learn for the lightweight recruiter-side models, and the Sentence Transformers library for Qwen3 text embeddings.

% ============================================================
\section{User-Side Recommendation Experiments}
\label{sec:exp_user}
% ============================================================

\subsection{Experiment Goal}
\label{sec:exp_user_goal}

The goal of the user-side experiments is to train and evaluate a two-tower model that can rank relevant jobs highly for cold-start users — users with no interaction history in the training period. The model is trained on Windows 1--4, validated on Windows 5--6, and evaluated on Window 7, where it must generalise to entirely new users and jobs. Success is measured primarily by \gls{ROC-AUC} during training, as this metric is least sensitive to the candidate pool size of the validation set combined with the practical utility of the model. The practical utility of the model is assessed qualitatively at inference time by inspecting recommendations for a sample of test users against the full Window 7 job pool.
 
\subsection{Ablation Study}
\label{sec:exp_user_ablation}
 
To determine the optimal model configuration, a systematic ablation study is conducted comparing two design choices: whether to include location features (city and state) and whether to apply L2 normalisation to the output embeddings. Each configuration is trained for 3 epochs under identical conditions and compared by validation \gls{ROC-AUC}. Table~\ref{tab:ablation_user} reports the results.
 
\begin{table}[htbp]
    \centering
    \caption{Ablation study results after 3 training epochs (validation \gls{ROC-AUC}).}
    \label{tab:ablation_user}
    \begin{tabular}{llccc}
        \toprule
        \textbf{Location Features} & \textbf{L2 Norm.} & \textbf{Val Loss} & \textbf{Accuracy} & \textbf{AUC} \\
        \midrule
        Excluded & No  & 0.3513 & 84.60\% & 0.8591 \\
        Excluded & Yes & 0.4687 & 84.18\% & 0.7954 \\
        Included & No  & 0.2317 & 91.24\% & \textbf{0.9365} \\
        Included & Yes & 0.4693 & 84.13\% & 0.7950 \\
        \bottomrule
    \end{tabular}
\end{table}
TODO: Explain findings, recheck table values
%Two findings emerge from the quantitative results. First, L2 normalisation consistently hurts performance regardless of whether location is included. Without normalisation, the dot product score is unbounded, allowing the model to encode match confidence in both the direction and magnitude of the embedding vectors. Normalising to unit norm removes the magnitude, reducing the model's representational capacity.
 
%Second, including location features with a shared vocabulary shows a substantial quantitative improvement — \gls{ROC-AUC} rises from 0.8591 to 0.9365. This is at first surprising, since city-based negative sampling should prevent location from being learned as a direct discriminative signal. The improvement is better explained by the additional model capacity: the city and state embedding layers allow the model to capture latent associations between location and job market characteristics, such as industries that cluster in particular cities.
 
%However, the quantitative advantage of including location features does not translate to better real-world recommendations. The qualitative evaluation in Section~\ref{sec:exp_user_inference} shows that the location-included model produces geographically clustered recommendations driven by job density rather than semantic career fit — recommending jobs from high-density cities such as Pembroke Pines or Miramar to users located in Orlando or Hollywood. This discrepancy arises from a fundamental mismatch between the validation and inference settings. City-based negative sampling controls for location in the validation set — both positive and negative jobs are drawn from the same city — but at inference the model ranks against all jobs across all cities. A model that has learned to exploit location signals therefore performs well on the location-controlled validation set but generalises poorly to the full inference setting.
 
%This is a concrete illustration of how offline evaluation metrics can be misleading proxies for real-world recommendation quality when the evaluation protocol does not match the deployment scenario. The location-excluded model achieves lower \gls{ROC-AUC} on the biased validation set but produces qualitatively superior recommendations by focusing exclusively on semantic job-fit signals — career history, education and job title similarity — which generalise across cities.
 
%Based on both quantitative and qualitative evidence, the final model uses \textbf{no location features and no L2 normalisation}. The importance of the shared vocabulary finding is preserved as a negative result: it confirms that location encoding must be consistent across towers if location is used, but the qualitative evaluation shows location should not be used at all in this setting.
 
\subsection{Final Model Results}
\label{sec:user_results}
 
The final model is trained without location features and without L2 normalisation, as determined by the combined quantitative and qualitative ablation analysis. The model is trained for a maximum of 50 epochs with early stopping (patience = 5). Table~\ref{tab:final_results_user} reports the validation set results at the best checkpoint.
 
For reference, Table~\ref{tab:model_comparison} compares the location-included and location-excluded configurations at full training convergence, making explicit the quantitative cost of excluding location features alongside the qualitative benefit established in Section~\ref{sec:exp_user_inference}.
 
\begin{table}[htbp]
    \centering
    \caption{Final model results on the validation set (best checkpoint, no location features).}
    \label{tab:final_results_user}
    \begin{tabular}{lr}
        \toprule
        \textbf{Metric} & \textbf{Value} \\
        \midrule
        Training Loss   & TODO \\
        Validation Loss & TODO \\
        Accuracy        & TODO\% \\
        AUC-ROC         & TODO \\
        \midrule
        Hit@5           & TODO \\
        Recall@5        & TODO \\
        NDCG@5          & TODO \\
        Hit@10          & TODO \\
        Recall@10       & TODO \\
        NDCG@10         & TODO \\
        Hit@20          & TODO \\
        Recall@20       & TODO \\
        NDCG@20         & TODO \\
        \bottomrule
    \end{tabular}
\end{table}
 
\begin{table}[htbp]
    \centering
    \caption{Comparison of location-included vs location-excluded models at full convergence.}
    \label{tab:model_comparison}
    \begin{tabular}{lcccc}
        \toprule
        \textbf{Configuration} & \textbf{Val Loss} & \textbf{Accuracy} & \textbf{AUC} & \textbf{Epochs} \\
        \midrule
        Location included, no L2 norm & 0.2233 & 91.76\% & 0.9456 & 11 \\
        Location excluded, no L2 norm & TODO   & TODO\%  & TODO   & TODO \\
        \bottomrule
    \end{tabular}
\end{table}
 
The lower \gls{ROC-AUC} of the location-excluded model reflects the reduced capacity to exploit location-based patterns in the validation set, as discussed in Section~\ref{sec:exp_user_ablation}. The qualitative evaluation in Section~\ref{sec:exp_user_inference} demonstrates that this quantitative cost is offset by substantially better semantic generalisation at inference time.
 
\subsection{Qualitative Inference Evaluation}
\label{sec:exp_user_inference}
 
To assess real-world recommendation quality, the final model is run at inference time against all jobs from Window~7 (approximately 40,000 jobs) for a sample of test users. Geographic post-processing is applied so that jobs in the same state as the user are ranked ahead of out-of-state jobs. Two representative cases are discussed below.
 
\paragraph{User Y}
TODO
 
\paragraph{User X}
TODO
 
\paragraph{Limitation: geographic clustering with location features.}
For completeness, the location-included model was also evaluated qualitatively. For a Florida-based user (Hollywood, FL) with a 17-year insurance background, the location-included model recommended generic sales and marketing roles clustered entirely in Miramar, FL — a city near Miami with high job density in the dataset but geographically distant from the user. Similarly, an Orlando user received all top-10 recommendations from Pembroke Pines, FL. This geographic clustering confirms the analysis in Section~\ref{sec:exp_user_ablation}: the location-included model exploits city-level job density patterns that inflate validation \gls{ROC-AUC} but produce poor inference results. The location-excluded model does not exhibit this behaviour, as demonstrated by Users 650340 and 1240342 above.
 
% ============================================================
\section{Recruiter-Side Recommendation Experiments}
\label{sec:exp_recruiter}
% ============================================================
 
\subsection{Experiment Goal}
\label{sec:exp_recruiter_goal}
TODO
 
\subsection{Experiment Setting}
\label{sec:exp_recruiter_setting}
TODO
 
\subsection{Results}
\label{sec:exp_recruiter_results}
TODO
 
\begin{table}[htbp]
    \centering
    \caption{Recruiter-side model comparison (5-fold cross-validation).}
    \label{tab:recruiter_results}
    \begin{tabular}{lrrrrr}
        \toprule
        \textbf{Model} & \textbf{Accuracy} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} & \textbf{AUC} \\
        \midrule
        Logistic Regression & TODO & TODO & TODO & TODO & TODO \\
        Random Forest       & TODO & TODO & TODO & TODO & TODO \\
        XGBoost             & TODO & TODO & TODO & TODO & TODO \\
        Ensemble            & TODO & TODO & TODO & TODO & TODO \\
        \bottomrule
    \end{tabular}
\end{table}
 
% ============================================================
\section{Congestion Balancing Experiments}
\label{sec:exp_congestion}
% ============================================================
 
\subsection{Experiment Goal}
\label{sec:exp_congestion_goal}
TODO
 
\subsection{Experiment Setting}
\label{sec:exp_congestion_setting}
TODO
 
\subsection{Job-Side Congestion Analysis}
\label{sec:exp_job_congestion}
TODO
 
\subsection{User-Side Congestion Analysis}
\label{sec:exp_user_congestion}
TODO
 
\subsection{Hybrid Balancing Results}
\label{sec:exp_hybrid_results}
TODO