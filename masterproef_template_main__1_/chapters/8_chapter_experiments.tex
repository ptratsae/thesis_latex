\chapter{Experiments and Results}
\label{chap:experiments}
This chapter describes the experimental evaluation of the systems developed in previous chapters. First, the shared experiment settings are described in Section~\ref{sec:exp_settings} which includes the custom dataset split, dataset statistics, and hardware configuration. Then, experiments are presented for the user-side recommendation system (Section~\ref{sec:exp_user}), the recruiter-side recommendation system (Section~\ref{sec:exp_recruiter}), and the congestion balancing strategies (Section~\ref{sec:exp_congestion}).

\section{General Experiments Setting}
\label{sec:exp_settings}

\subsection{Dataset}
\label{sec:exp_dataset}
All experiments are conducted on the CareerBuilder Job Recommendation dataset~\cite{careerbuilderdataset}, consisting of real-world job applications, user profiles and job postings from CareerBuilder.com over a 13-week period. The dataset is organised into seven consecutive 13-day temporal windows where users apply to jobs which are active in the same window. Table~\ref{tab:dataset_stats} summarises the scale of the full dataset.

\begin{table}[htbp]
    \centering
    \caption{CareerBuilder dataset statistics.}
    \label{tab:dataset_stats}
    \begin{tabular}{lr}
        \toprule
        \textbf{Statistic} & \textbf{Value} \\
        \midrule
        Total unique users       & 389,708 \\
        Total unique jobs        & 1,091,923 \\
        Total applications       & 1,603,111 \\
        Interaction matrix sparsity & 99.9996\% \\
        \bottomrule
    \end{tabular}
\end{table}

The extremely high sparsity — fewer than one in 200,000 user-job pairs has a recorded interaction — means thatcollaborative approaches relying on interaction density are infeasible. The two-tower architecture instead uses interactions as a training signal while generalising through user and job profile features, making it robust to sparse data.

\subsection{Custom Train/Validation/Test Split}
\label{sec:custom_split}

\begin{figure}[ht]
    \centering
    \begin{tikzpicture}[
        window/.style={draw, minimum width=1.4cm, minimum height=0.8cm, font=\footnotesize, fill=white},
        label_node/.style={font=\small\bfseries, align=center}
    ]
    \node [window] (w1) at (0,0)   {W1};
    \node [window] (w2) at (1.5,0) {W2};
    \node [window] (w3) at (3,0)   {W3};
    \node [window] (w4) at (4.5,0) {W4};
    \node [window] (w5) at (6.5,0) {W5};
    \node [window] (w6) at (8,0)   {W6};
    \node [window] (w7) at (9.5,0) {W7};

    \draw [dashed, line width=1.5pt, red!80!black] (5.5, 1.2) -- (5.5, -2.5)
        node[below, font=\footnotesize\itshape, text=red!80!black] {Temporal Boundary};
    \draw [-{Stealth}, thick] (-1, -0.7) -- (10.5, -0.7) node[right] {Time};

    \draw [decorate, decoration={brace, amplitude=10pt}]
        (-0.7, 0.6) -- (5.2, 0.6)
        node [midway, yshift=0.6cm, label_node, color=blue!70!black] {Training Set (Windows 1--4)};
    \node [align=center, font=\scriptsize, color=gray!80!black] at (2.25, 1.6) {Historical Data};

    \draw [decorate, decoration={brace, amplitude=10pt}]
        (5.8, 0.6) -- (10.2, 0.6)
        node [midway, yshift=0.6cm, label_node, color=green!50!black] {Validation/Test Set (Windows 5--7)};
    \node [align=center, font=\scriptsize, color=gray!80!black] at (8, 1.6) {Cold-Start Evaluation};

    \node [draw, rounded corners, fill=gray!5, font=\tiny, align=left] at (2.25, -1.8) {
        \textbullet\ All users/jobs included \\
        \textbullet\ Historical behaviour used for training
    };
    \node [draw, rounded corners, fill=gray!5, font=\tiny, align=left] at (8, -1.8) {
        \textbullet\ No overlap with training users \\
        \textbullet\ Simulates realistic deployment
    };
    \end{tikzpicture}
    \caption{Custom temporal split for the CareerBuilder dataset. Unlike the original within-window split, this approach divides by window boundary, ensuring a strict cold-start evaluation scenario.}
    \label{fig:custom_temporal_split}
\end{figure}

Rather than using the original within-window train/test labels provided by the dataset, a temporal split across windows is applied (Figure~\ref{fig:custom_temporal_split}). Windows 1--4 form the training set and Windows 5--7 form the validation and test set, with Windows 5--6 used for validation and Window 7 reserved for final evaluation.

Users marked \texttt{Split=Test} are excluded entirely (22,838 users). These users made five or more applications during the withheld 4-day test period of the original competition. Of these, 43\% have no recorded interactions in the training period whatsoever, as their only observable activity falls within the withheld test period. Including them would introduce users with structurally missing interaction records into the evaluation set, so they are excluded entirely.

This design has three consequences. First, there is a strict temporal boundary: the model never sees any interaction from the evaluation period during training. Second, the evaluation is a genuine cold-start scenario — no user appears in both the training and evaluation sets. Third, using all active users from each window group (rather than filtering by activity thresholds) avoids biasing the evaluation towards highly-engaged users. Table~\ref{tab:split_stats} shows the resulting split sizes.

\begin{table}[htbp]
    \centering
    \caption{Dataset split statistics after applying the custom temporal split.}
    \label{tab:split_stats}
    \begin{tabular}{lrrr}
        \toprule
        \textbf{Split} & \textbf{Users} & \textbf{Jobs} & \textbf{Applications} \\
        \midrule
        Training   (W1--W4) & 189,657 & 223,600 & 881,249  \\
        Validation (W5--W6) &  78,428 &  86,196 & 367,038  \\
        Test       (W7)     &  39,948 &  39,946 & 169,227  \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{Hardware and Software Configuration}
\label{sec:hardware}

All experiments are run on the Artificial Intelligence and Data Analytics Research Group (AIDA) high-performance computing cluster via SSH. Training is performed on a single NVIDIA A40 GPU (46~GB VRAM). The software stack uses Python with PyTorch as the deep learning framework, scikit-learn for the lightweight recruiter-side models, and the Sentence Transformers library for Qwen3 text embeddings.

% ============================================================
\section{User-Side Recommendation Experiments}
\label{sec:exp_user}
% ============================================================
 
\subsection{Experiment Goal}
\label{sec:exp_user_goal}
The goal of the user-side experiments is to train and evaluate a two-tower model that can rank relevant jobs highly for cold-start users — users with no interaction history in the training period. The model is trained on Windows 1--4, validated on Windows 5--6, and evaluated on Window 7 by doing sanity checks, where it must generalise to entirely new users and jobs. Success is measured primarily by \gls{ROC-AUC} during training, as this metric is least sensitive to the candidate pool size of the validation set combined with the practical utility of the model. The practical utility of the model is assessed qualitatively at inference time by inspecting recommendations for a sample of test users against the full Window 7 job pool.
 
\subsection{Subsampling and Negative Sampling Strategy}
\label{sec:negative_sampling}
From the 189,657 users in the training set, a random sample of 25,000 users per window is drawn to create a manageable training set of 100,000 users. With a median of 2 applications per user, this results in approximately 200,000 positive user-job pairs for training. The validation set is not subsampled and contains all 78,428 users having approximately 156,856 positive user-job pairs for validation. 

Because the training data contains only positive interactions (jobs a user applied to), negative examples must be constructed explicitly. A random negative sampling approach — drawing jobs uniformly at random — would produce trivially easy negatives: a software engineer from Texas would be contrasted with nursing jobs in Maine, and the model could separate them on geography or title alone without learning meaningful preference signals.
 
The negative sampling strategy is therefore designed to produce \emph{hard} and \emph{realistic} negatives. For each positive pair $(u, j^+)$, four negatives are sampled according to a \textbf{2:1:1 ratio}:
\begin{itemize}
    \item \textbf{2 cross-state negatives}: jobs from states other than the user's own state, sampled from the same temporal window. These teach the model that geographic fit is a genuine signal.
    \item \textbf{1 same-state negative}: a job from the user's state but a different city, active in the same window. This forces the model to look beyond state-level location and discriminate on career content.
    \item \textbf{1 same-city negative}: a job posted in the user's city but one the user did not apply to. These are the hardest negatives, as they share the user's location and are still unapplied to, pressuring the model to learn semantic fit.
\end{itemize}
 
The 2:1:1 strategy ensures the training distribution is representative of the real inference setting, where the model must rank jobs across the entire country, and resolves this clustering artefact as demonstrated in Section~\ref{sec:exp_user_inference}. 

\subsection{Hyperparameters and Training Settings}
\label{sec:exp_user_hyperparameters}
The model is trained for a maximum of 50 epochs with early stopping (patience~= 5) based on validation loss. The Adam optimiser is used with a learning rate of $10^{-3}$ and a batch size of 1024. The embedding dimension $d$ is set to 64.
\subsection{Ablation Study}
\label{sec:exp_user_ablation}
 
To determine the optimal model configuration, a systematic ablation study is conducted comparing two design choices: whether to include location features (city and state) and whether to apply L2 normalisation to the output embeddings. All configurations are trained with the 2:1:1 negative sampling strategy described in Section~\ref{sec:negative_sampling}, with early stopping (patience~= 5). Table~\ref{tab:ablation_user} reports the results at the best validation checkpoint for each configuration.
 
A note on interpreting the ranking metrics: Hit@K, Recall@K and NDCG@K are computed against the validation set's balanced candidate pool (positive and sampled negative jobs per user) rather than the full job catalogue. For example if a user has applied to 2 jobs, the metrics are computed against a pool of 2 positive jobs and 8 sampled negative jobs using the negative sampling strategy discussed in Section~\ref{sec:negative_sampling}. As discussed in Section~\ref{sec:prelim_metrics}, this makes their absolute values considerably more optimistic than what would be observed at full-scale inference. AUC-ROC is therefore used as the primary comparison criterion, as it is less sensitive to this pool-size effect. This caveat applies equally to the final model results in Section~\ref{sec:user_results}.
 
\begin{table}[htbp]
    \centering
    \caption{Ablation study results at best validation checkpoint, early stopping (patience~= 5).}
    \label{tab:ablation_user}
    \begin{tabular}{llcccc}
        \toprule
        \textbf{Location Features} & \textbf{L2 Norm.} & \textbf{Val Loss} & \textbf{Accuracy} & \textbf{AUC} & \textbf{Epochs} \\
        \midrule
        Included & No  & 0.2706 & 88.90\% & \textbf{0.9242} & 19 \\
        Excluded & No  & 0.3463 & 84.92\% & 0.8641 &  9 \\
        Included & Yes & 0.4852 & 82.15\% & 0.7331 &  8 \\
        Excluded & Yes & 0.4892 & 81.73\% & 0.7426 &  4 \\
        \bottomrule
    \end{tabular}
\end{table}
 
Two findings emerge from the results. First, \textbf{L2 normalisation consistently hurts performance} regardless of whether location features are present. Normalising output embeddings to unit norm removes their magnitude, which prevents the model from encoding match confidence in the scale of the dot product. Without normalisation, the magnitude acts as an additional degree of freedom that the model can use to express certainty, benefiting both the loss and the ranking quality. This is an interesting finding considering that cosine similarity is a common practice to calculate embedding similarity \cite{you2025semanticsanglecosinesimilarity}.
 
Second, \textbf{including location features improves performance substantially} — \gls{ROC-AUC} rises from 0.8641 to 0.9242. The 2:1:1 strategy exposes the model to geographically diverse negatives during training. This prevents the model from learning city-density patterns and instead allows it to genuinely learn state-level geographic preference as a meaningful signal. The result is that the location-included model not only scores higher offline but also produces geographically sensible recommendations at inference, as confirmed by the qualitative evaluation in Section~\ref{sec:exp_user_inference}.
 
Based on both quantitative and qualitative evidence, the final model \textbf{includes location features and applies no L2 normalisation}. A complete breakdown of all four configurations across all ranking metrics is provided in Table~\ref{app:tab:full_ablation} in the Appendix.
 
\subsection{Final Model Results}
\label{sec:user_results}
 
The final model is trained \textbf{with location features and without L2 normalisation}, as determined by the ablation study in Section~\ref{sec:exp_user_ablation}. The model is trained for a maximum of 50 epochs with early stopping (patience~= 5) and converges at epoch~19. Figure~\ref{fig:trainval} shows the 
training and validation loss curves across epochs.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.75\textwidth]{images/train_val_loss.png}
    \caption{Training and validation loss curves for the final two-tower model. 
The best validation checkpoint is saved at epoch~19. Training continues for 
5 further epochs (patience~= 5) before early stopping halts at epoch~24.}
    \label{fig:trainval}
\end{figure}

Table~\ref{tab:final_results_user} reports the validation set results at the best checkpoint.
 
\begin{table}[H]
    \centering
    \caption{Final model results on the validation set (best checkpoint, location features included, no L2 norm).}
    \label{tab:final_results_user}
    \begin{tabular}{lr}
        \toprule
        \textbf{Metric} & \textbf{Value} \\
        \midrule
        Training Loss   & 0.2006 \\
        Validation Loss & 0.2706 \\
        Accuracy        & 88.90\% \\
        AUC-ROC         & 0.9242 \\
        \midrule
        Hit@1           & 0.8202 \\
        Recall@1        & 0.4193 \\
        NDCG@1          & 0.8202 \\
        Hit@3           & 0.9740 \\
        Recall@3        & 0.7213 \\
        NDCG@3          & 0.8525 \\
        Hit@5           & 0.9922 \\
        Recall@5        & 0.8291 \\
        NDCG@5          & 0.8684 \\
        Hit@10          & 0.9979 \\
        Recall@10       & 0.9239 \\
        NDCG@10         & 0.8871 \\
        \bottomrule
    \end{tabular}
\end{table}
 
\subsection{Qualitative Inference Evaluation}
\label{sec:exp_user_inference}
 
To assess real-world recommendation quality, the final model is run at inference time against all jobs from Window~7 (approximately 40,000 jobs) for a sample of test users. As described in Section~\ref{sec:inference_postprocessing}, no geographic post-processing boost is applied — same-state jobs surface organically from the model's learned preferences. Two representative cases are discussed below.
 
\paragraph{User 668904 — clear career signal, strong geographic coherence.}
This user is based in Deer Park, TX, holds a high school diploma and has 18 years of work experience. Their job history consists entirely of accounts payable and administrative finance roles: Accounts Payable Analyst, Administrative Assistant, and AP/AR support positions. In Window~7 the user applied to a single job — a Payroll Specialist role in Houston, TX — which the model scores at 3.65. All ten recommended jobs are in Houston and fall squarely within the same financial-administrative cluster: Senior Accounts Payable Specialist, AP/AR Specialist, Accounting Assistant, Accounts Payable Clerk, and similar titles. This is a clean demonstration of the model working as intended: a coherent career signal in the user embedding produces semantically tight, geographically appropriate recommendations with no location post-processing required.

\begin{table}[H]
    \centering
    \caption{Profile summary for User 668904.}
    \label{tab:user668904_profile}
    \small
    \begin{tabular}{ll}
        \toprule
        \textbf{Attribute} & \textbf{Value} \\
        \midrule
        Location            & Deer Park, TX \\
        Education           & High School \\
        Experience          & 18 years \\
        Currently Employed  & Yes \\
        Recent Job History  & Accounts Payable Analyst, Administrative Assistant, \\
                            & AP/AR Support, Document Control Clerk \\
        Window 7 Application & Payroll Specialist (Houston, TX) — score 3.65 \\
        \bottomrule
    \end{tabular}
\end{table}
 
\begin{table}[H]
    \centering
    \caption{Top-10 recommendations for User 668904 against the full Window~7 job pool.}
    \label{tab:user668904_recs}
    \small
    \begin{tabular}{clll r}
        \toprule
        \textbf{Rank} & \textbf{Job Title} & \textbf{City} & \textbf{State} & \textbf{Score} \\
        \midrule
        1  & Senior Accounts Payable Specialist       & Houston & TX & 4.48 \\
        2  & Accounting Payable / Receivable Specialist & Houston & TX & 4.45 \\
        3  & Accounting Assistant                     & Houston & TX & 4.42 \\
        4  & Accounts Payable                         & Houston & TX & 4.40 \\
        5  & Accounting Clerk — AR                    & Houston & TX & 4.36 \\
        6  & Accounts Payable Clerk                   & Houston & TX & 4.35 \\
        7  & Part Time Bookkeeper                     & Houston & TX & 4.31 \\
        8  & Accounting Clerk                         & Houston & TX & 4.30 \\
        9  & Collection and Reimbursement Processor   & Houston & TX & 4.24 \\
        10 & AP Accountant                            & Houston & TX & 4.23 \\
        \bottomrule
    \end{tabular}
\end{table}

\paragraph{User 831532 — cold-start robustness with no job history.}
This user is based in Philadelphia, PA, holds a high school diploma and has only 2 years of recorded experience, with no job history entries in the dataset. This represents a genuine cold-start scenario: the model has no career trajectory to draw on and must rely entirely on profile features. Despite this, the top-10 recommendations are geographically coherent across the PA/NJ/DE tri-state area — the natural labour market for a Philadelphia resident — and semantically consistent with the applied-to jobs, which include residential youth care and community support roles. Notably, one of the three jobs the user actually applied to in Window~7 (Residential Youth Care Workers Needed, Conshohocken PA) appears at rank~7 in the recommendation list, despite being ranked against approximately 40,000 jobs. This result illustrates the value of the two-tower architecture in cold-start conditions: even without job history, the combination of location, education and basic profile features is sufficient to produce meaningful recommendations. A full breakdown of all ablation configurations across all metrics is provided in Table~\ref{app:tab:full_ablation} in the Appendix.
 
\begin{table}[H]
    \centering
    \caption{Profile summary for User 831532.}
    \label{tab:user831532_profile}
    \small
    \begin{tabular}{ll}
        \toprule
        \textbf{Attribute} & \textbf{Value} \\
        \midrule
        Location            & Philadelphia, PA \\
        Education           & High School \\
        Experience          & 2 years \\
        Currently Employed  & Yes \\
        Recent Job History  & None recorded \\
        Window 7 Applications & Program Associate (Haddonfield, NJ) — score 2.19 \\
                              & Immediate Openings F/T (Bala Cynwyd, PA) — score 2.81 \\
                              & Residential Youth Care Workers (Conshohocken, PA) — score 3.58 \\
        \bottomrule
    \end{tabular}
\end{table}
 
\begin{table}[H]
    \centering
    \caption{Top-10 recommendations for User 831532 against the full Window~7 job pool. $^\dagger$Indicates a job the user actually applied to.}
    \label{tab:user831532_recs}
    \small
    \begin{tabular}{clll r}
        \toprule
        \textbf{Rank} & \textbf{Job Title} & \textbf{City} & \textbf{State} & \textbf{Score} \\
        \midrule
        1  & Customer Service Representative          & Broomall      & PA & 4.17 \\
        2  & Nanny / Childcare / Teacher              & Madison       & NJ & 4.06 \\
        3  & Concierge / Receptionist                 & Madison       & NJ & 3.93 \\
        4  & Store Management                         & Christiana    & DE & 3.82 \\
        5  & Shelter Staff (Weekend \& Substitute)    & Wilmington    & DE & 3.68 \\
        6  & Community Life Assistant                 & Broomall      & PA & 3.61 \\
        7  & Residential Youth Care Workers Needed$^\dagger$ & Conshohocken & PA & 3.58 \\
        8  & Medical Reimbursement Collections Assoc. & Moorestown   & NJ & 3.56 \\
        9  & Daycare / Nursery Worker                 & Wilmington    & DE & 3.50 \\
        10 & Store Management                         & Cherry Hill   & NJ & 3.50 \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{Limitations}
\label{sec:exp_user_limitations}
Despite strong quantitative performance and qualitatively sensible recommendations for users with stable career histories, 3 structural limitations of the current 
design are worth noting.

\paragraph{Overfitting to the training distribution.}
Figure~\ref{fig:trainval} reveals a growing divergence between training and validation loss after approximately epoch~5. While validation loss plateaus 
around $0.27$, training loss continues to decrease to $0.20$, suggesting the model begins to overfit to the training windows. This is consistent with the 
cold-start evaluation setup: the model is trained on Windows~1--4 and evaluated on entirely new users and jobs from Windows~5--7, meaning the distribution shift 
between train and evaluation is structural rather than random. Dropout and batch normalisation partially mitigate this, but the gap suggests the model has 
memorised aspects of the training user-job distribution that do not fully generalise. Possible mitigations include stronger regularisation, increased dropout rates, or data augmentation techniques such as feature masking during 
training.
\paragraph{Temporally flat job history representation.}
The job history embedding is produced by encoding the three most recent job titles 
as a single concatenated string and passing it through the Qwen encoder. This means 
all three titles contribute equally to the resulting vector, regardless of how recent 
they are and the duration of their employment. Recent jobs contain more signal about the user's current preferences and are more predictive of their next move, while older jobs may reflect past interests or even noise. The duration of each job is also relevant — a user with a 10-year tenure as a software engineer followed by a 3-month stint in retail likely has different preferences than a user with the opposite pattern. However, the CareerBuilder dataset does not provide employment duration in the user history, making this signal inaccessible to the current model.

A natural extension would be to embed each title independently and compute a weighted 
sum with recency weights, for example:
\begin{equation}
    \mathbf{e}_{\text{history}} = 0.5 \cdot \mathbf{e}_{t_1} 
                                + 0.3 \cdot \mathbf{e}_{t_2} 
                                + 0.2 \cdot \mathbf{e}_{t_3}
\end{equation}
where $\mathbf{e}_{t_1}$ is the embedding of the most recent title. This would 
encode both the content and the direction of career movement into the user 
representation. This limitation was not apparent in the two cases presented in 
Section~\ref{sec:exp_user_inference}, as both users had coherent and stable 
career histories. It is expected to surface most clearly in evaluation on users 
with mixed or transitioning profiles.
% TODO - add example of a user with a mixed/transitioning profile and how the current design fails to capture it

\paragraph{City and State embedding sparsity.}
City and state are encoded as categorical features with learned embeddings. Cities and states that appear rarely in Windows~1--4 have fewer training examples and therefore an undertrained embedding. For users located in such cities, the geographic signal 
is weak and the model compensates by relying more heavily on the job history 
representation — which then exposes the limitation above. This effect is expected 
to be most pronounced for users outside major metropolitan areas.
% TODO - add statistics on city frequency distribution and how many users are affected by this sparsity issue

\newpage
\section{Recruiter-Side Recommendation Experiments}
\label{sec:exp_recruiter}
% ============================================================
 
\subsection{Experiment Goal}
\label{sec:exp_recruiter_goal}
 
The recruiter-side experiments address two design choices that must be made before a final model can be deployed. The first is \textbf{classifier selection}: given the engineered feature set described in Section~\ref{sec:recruiter_features}, which model family best exploits it? The second is \textbf{negative strategy selection}: given the chosen classifier, does the choice of negative construction — \gls{LLM}-evaluated hard negatives from the applicant pool, synthetically sampled non-applicants or a combination of both — have a measurable effect on ranking quality, and which strategy yields the more useful model? These two questions are answered sequentially in Sections~\ref{sec:exp_recruiter_model_comparison} and~\ref{sec:exp_recruiter_negatives}. Success is assessed both quantitatively via the metrics introduced in Section~\ref{sec:prelim_classification}, and qualitatively through sanity checks.
 
\subsection{Data Generation}
\label{sec:exp_recruiter_datagen}
 
Because no ground-truth recruiter feedback exists in the dataset, training labels must be generated synthetically. The method is described in Section~\ref{sec:recruiter_data_gen}; this section provides the implementation details specific to this experiment.
 
\paragraph{Labeling setup}
Labels are generated using the Qwen3.5 (35B) model served locally via the Ollama API. For computational feasibility, a sample of jobs is drawn from Windows~1--4; for each job, up to 20 applicants are evaluated in a single batched prompt. The \gls{LLM} receives the job description alongside all candidate profiles in the batch and returns a structured JSON response. The final prompt used is:

{\small
\begin{verbatim}
Task: You are an HR recruiter. Evaluate how well each person fits the job
based on their profile and the job description.
(IMPORTANT: Treat each job-user pair independently.)

Assign a fit level:
- "Very Good Fit": Meets 100% of Must-Haves AND 50%+ of Nice-to-Haves
  AND 0 Deal-Breakers.
- "Good Fit": Meets 100% of Must-Haves (OR misses 1 Must-Have but
  compensates with extensive relevant experience) AND 0 Deal-Breakers.
- "Poor Fit": Missing 2+ Must-Haves OR has exactly 1 Deal-Breaker.
- "Very Poor Fit": Missing 3+ Must-Haves OR has 2+ Deal-Breakers.

Return a JSON object:
{"results": [{"candidate_id": <id>, "must_haves": "...",
  "nice_to_haves": "...", "deal_breakers": "...",
  "reasoning": "...", "fit_level": "Good Fit"}]}

Notes:
- Be strict. Only candidates that truly meet requirements get Good Fit.
- Be objective. Focus on concrete matches/mismatches.
- If Degree, Major, Experience or History is empty/NaN, assume the
  candidate does not have that feature.
- Assume all applicants are 18+, available, and physically able.

Job Description: {job_desc}
Candidate Profiles: {profiles_text}
\end{verbatim}
}

This prompt contains explicit rules, notes and output format to ensure consistency. The prompt starts with a clear task definition and a precise fit level rules. This is followed by detailed instructions on the expected output format, including the required JSON structure and specific content for each field. By providing fields for must-haves, nice-to-haves, deal-breakers and reasoning, the prompt encourages the model to reason and explain its decisions rather than just outputting a label. Then extra notes are established to guide the model towards a more conservative and specific evaluation style. These notes were iteratively defined by inspecting model outputs and adjusting the prompt to address common issues. For example some rows have missing values in key features like Degree or Experience, and without explicit instructions the model would often ignore these and assume the candidate had a degree or relevant experience, leading to overly optimistic fit levels. By instructing the model to treat missing values as an absence of that feature, it produces more realistic assessments. Finaly both job descriptions and candidate profiles are included in the prompt. 

An example output for a single candidate is shown below:
 
\begin{listing}[H]
\begin{minted}{json}
{
  "results": [
    {
      "candidate_id": 349089,
      "must_haves": "Associates degree, 6mo experience/leadership,
                     valid license, work authorisation",
      "nice_to_haves": "Bachelors degree, sales/CS background",
      "deal_breakers": "None identified",
      "reasoning": "Candidate profile shows degree and experience as
                    empty. Job requires Associates degree and 6 months
                    experience. Missing these core requirements.",
      "fit_level": "Poor Fit"
    }
  ]
}
\end{minted}
\caption{Example LLM JSON output for a single candidate-job evaluation.}
\label{lst:llm_json_output}
\end{listing}
 
The structured output format enforces consistency: every response contains an explicit must-haves assessment, a reasoning trace, and a fit level drawn from a fixed four-value vocabulary. Responses that fail JSON parsing or contain unexpected fit level values are discarded.
 
\paragraph{Label distribution.}
The resulting dataset contains \textbf{5,896} labeled pairs across \textbf{1511} unique jobs. Table~\ref{tab:recruiter_label_dist} shows the fit level distribution. The negative class (Poor and Very Poor Fit) accounts for 54.1\% of pairs, slightly outnumbering positives. This reflects the conservative evaluation style enforced by the prompt: missing values in profile fields are treated as absent features, and the model is instructed to be strict (see Section~\ref{sec:exp_recruiter_datagen}). Poor Fit is the single most frequent label at 41.9\%, consistent with many applicants lacking one or more of the explicit must-haves extracted from the job description. Class weights are still applied during training to ensure each class contributes proportionally to the loss, with weights set inversely proportional to class frequency.
 
\begin{table}[htbp]
    \centering
    \caption{Label distribution of the LLM-generated dataset.}
    \label{tab:recruiter_label_dist}
    \begin{tabular}{lrr}
        \toprule
        \textbf{Fit Level} & \textbf{Count} & \textbf{Percentage} \\
        \midrule
        Very Good Fit &   777 & 13.18\% \\
        Good Fit      & 1,928 & 32.70\% \\
        Poor Fit      & 2,472 & 41.93\% \\
        Very Poor Fit &   719 & 12.19\% \\
        \midrule
        \textbf{Total positive ($y=1$)} & 2,705 & 45.88\% \\
        \textbf{Total negative ($y=0$)} & 3,191 & 54.12\% \\
        \textbf{Grand total}            & 5,896 & 100\% \\
        \bottomrule
    \end{tabular}
\end{table}
 
\paragraph{Validation set}
A separate held-out small labeled set is generated for the qualitative sanity check targeting a disjoint sample of jobs from Windows~5--6. This set contains only real applicants with their \gls{LLM} labels — no synthetic negatives are added — providing a clean probe of whether the model correctly ranks good fits above poor ones among actual candidates on completely unseen jobs.
 
\subsection{Experiment Setting}
\label{sec:exp_recruiter_setting}
 
\paragraph{Data split}
To evaluate generalisation to unseen jobs, a group-based split by job identity is used rather than a random instance split. All unique jobs are randomly shuffled and partitioned into 80\% for cross-validation and final model training, and 20\% held-out test; all candidate-job pairs for a given job land in the same partition. The test set is used only once, after the final model has been chosen.
 
\paragraph{Negative sampling variants}
Three negative construction strategies are compared to isolate the effect of negative type:
\begin{itemize}
    \item \textbf{LLM negatives}: Poor and Very Poor Fit applicants as labeled by the \gls{LLM} — hard negatives from the real applicant pool.
    \item \textbf{Synthetic negatives}: non-applicants sampled randomly from the broader user pool, filtered so that their job history shares no keywords with the job title — easy negatives.
    \item \textbf{Combined negatives}: an equal 50/50 mix of LLM and synthetic negatives (1,352 of each), exposing the model to both hard and easy discrimination during training.
\end{itemize}
All variants use the same positive set. Each variant is evaluated on a held-out test set of the same 20\% of jobs, but the test pairs differ by strategy: for the LLM-negative model the test negatives are Poor/Very Poor Fit applicants; for the synthetic model they are random non-applicants; for combined, a mix of both. This means each model is tested in its own operating regime rather than against a single shared test set. Table~\ref{tab:recruiter_dataset_sizes} summarises the resulting dataset sizes.

\begin{table}[H]
    \centering
    \caption{Dataset composition for each negative construction strategy. The positive set is identical across all three variants.}
    \label{tab:recruiter_dataset_sizes}
    \begin{tabular}{lrrrr}
        \toprule
        \textbf{Strategy} & \textbf{Positives} & \textbf{LLM Negatives} & \textbf{Synthetic Negatives} & \textbf{Total} \\
        \midrule
        LLM (Poor / Very Poor Fit)       & 2,705 &  3,191 &      0 &  5,896 \\
        Synthetic (non-applicants) & 2,705 &      0 & 2,705 &  5,410 \\
        Combined       & 2,705 &  1,352 &  1,352 &  5,409 \\
        \bottomrule
    \end{tabular}
\end{table}
 
\paragraph{Training configuration}
Group 5-fold cross-validation is used for model selection, ensuring jobs are not split across folds. All four candidate classifiers (Logistic Regression, Random Forest, XGBoost, soft-voting ensemble) are evaluated under the same protocol. All features are standardised using a \texttt{StandardScaler} fitted on each training fold only, to prevent data leakage into the validation fold. \gls{TF-IDF} is likewise fitted on the training fold's job texts within each fold and then used to transform both the training and validation fold. \gls{PCA} is also refitted per fold on the training partition of the interaction matrix. The final model is retrained on the full 80\% pool with \gls{TF-IDF}, \gls{PCA}, and the scaler all fitted on that same 80\%, and all artifacts are serialised for inference.
 
\paragraph{Evaluation metrics}
Performance is reported using Accuracy, Precision, Recall, F1-score, and AUC-ROC as defined in Section~\ref{sec:prelim_metrics}. AUC-ROC is the primary model selection criterion. In the second stage of the experiment (negative strategy comparison), the same metrics are used to compare the final XGBoost model trained on each negative strategy, combined with a qualitative sanity check on different test sets to assess real-world ranking quality.
 
\subsection{Stage 1: Classifier Selection}
\label{sec:exp_recruiter_model_comparison}
 
All four candidate classifiers are compared on the \gls{LLM}-negative dataset, holding the training data fixed so that the only variable is the model type. The choice of this dataset is motivated by the fact that this is a harder classification task compared to the synthetic negative dataset. Table~\ref{tab:recruiter_results_cv} reports 5-fold cross-validation results; Table~\ref{tab:recruiter_results_test} reports the held-out test set performance of the selected model.

The Ensemble achieves the highest cross-validation AUC (0.696), but the gap over XGBoost (0.690) is 0.006 — well within the standard deviation of both models (0.017 and 0.019 respectively). This difference is not statistically meaningful. Random Forest is similarly tied on AUC (0.691) but has markedly lower Recall (0.528 vs.\ 0.588 for XGBoost), meaning it misses a larger fraction of genuinely qualified candidates. In a recruitment context, missing good candidates is the more costly error. XGBoost is therefore selected as the final classifier: it matches the Ensemble on AUC within noise, outperforms Random Forest on Recall, and provides native feature importance scores that support the interpretability of the model.

\begin{table}[htbp]
    \centering
    \small
    \caption{Recruiter-side model comparison (5-fold cross-validation, LLM negatives). Mean $\pm$ standard deviation across folds.}
    \label{tab:recruiter_results_cv}
    \begin{tabular}{lrrrrr}
        \toprule
        \textbf{Model} & \textbf{Accuracy} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} & \textbf{AUC} \\
        \midrule
        Logistic Regression & $0.598 \pm 0.009$ & $0.554 \pm 0.027$ & $0.589 \pm 0.054$ & $0.569 \pm 0.024$ & $0.638 \pm 0.017$ \\
        Random Forest       & $0.637 \pm 0.013$ & $0.619 \pm 0.036$ & $0.528 \pm 0.052$ & $0.567 \pm 0.021$ & $0.691 \pm 0.012$ \\
        XGBoost             & $0.642 \pm 0.012$ & $0.610 \pm 0.024$ & $0.588 \pm 0.047$ & $0.597 \pm 0.021$ & $0.690 \pm 0.019$ \\
        Ensemble            & $0.646 \pm 0.013$ & $0.619 \pm 0.032$ & $0.579 \pm 0.048$ & $0.596 \pm 0.019$ & $0.696 \pm 0.017$ \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{table}[htbp]
    \centering
    \small
    \caption{Final XGBoost model performance on the held-out test set (LLM negatives). The test set is evaluated only for the selected model to avoid selection bias.}
    \label{tab:recruiter_results_test}
    \begin{tabular}{lrrrrrr}
        \toprule
        \textbf{Model} & \textbf{Accuracy} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} & \textbf{AUC} \\
        \midrule
        XGBoost & 0.669 & 0.675 & 0.600 & 0.636 & 0.724 \\
        \bottomrule
    \end{tabular}
\end{table}


\subsubsection{Feature Importance}
\label{sec:exp_recruiter_feature_importance}

\paragraph{Interaction matrix PCA}
The embedding interaction matrix $\mathbf{m}_{uj} \in \mathbb{R}^{2d_{\text{text}}}$ has 200 dimensions. \gls{PCA} is applied to reduce the interaction matrix to $d_{\text{pca}} = 80$ components, retaining 92.9\% of the total variance. This compression is justified on two grounds. First, the Hadamard product and absolute difference of the same pair of embeddings share significant covariance structure, meaning a large fraction of the 200 dimensions are redundant — a finding confirmed by the high variance retention at only 40\% of the original dimensionality. Second, reducing the feature space lowers the risk of XGBoost fitting to noise in the interaction dimensions, even with 5,896 training pairs.

\paragraph{Feature importance analysis}
Table~\ref{tab:recruiter_feature_importance} shows the normalised feature 
importance for each of the three base classifiers on the last cross-validation 
fold. The \gls{PCA} interaction components dominate across all models — 
accounting for 69.2\%, 83.2\%, and 85.6\% of total importance in Logistic 
Regression, Random Forest, and XGBoost respectively. This confirms that the 
element-wise interaction between user and job Qwen embeddings captures the 
majority of the discriminative signal, validating the design choice of computing 
the full interaction matrix rather than relying solely on scalar cosine similarity.

Among the scalar features, the pattern differs by model family. In Logistic Regression, the Qwen cosine similarity (12.6\%) is the second most important feature, suggesting the linear model benefits from the direct scalar similarity signal. In Random Forest and XGBoost, \texttt{tfidf\_overlap} ranks higher than cosine similarity, suggesting that tree-based models extract more value from explicit keyword matching once the dominant PCA signal is accounted for. \texttt{TotalYearsExperience} ranks consistently across all three models. Both \texttt{degree\_match} and \texttt{full\_profile\_cosine\_sim} contribute negligibly across all classifiers --- under 2.6\% in the best case, and zero in XGBoost --- indicating they are subsumed by the PCA interaction and TF-IDF features. Despite their low importance, both features are retained in the final model. XGBoost's built-in regularisation already suppresses low-importance features during training, so removing them would not meaningfully change performance. Retaining them also preserves consistency with the feature engineering pipeline described in Section~\ref{sec:recruiter_features} and avoids the need for a separate retraining run to confirm that the delta is negligible.

\begin{table}[htbp]
    \centering
    \caption{Normalised feature importance across classifiers (last cross-validation 
    fold). PCA interaction components are aggregated into a single row.}
    \label{tab:recruiter_feature_importance}
    \begin{tabular}{lrrr}
        \toprule
        \textbf{Feature} & \textbf{Logistic Reg.} & \textbf{Random Forest} 
                         & \textbf{XGBoost} \\
        \midrule
        Interaction PCA (aggregated) & 69.2\% & 83.2\% & 85.6\% \\
        Qwen cosine similarity       & 12.6\% &  3.8\% &  1.9\% \\
        Total years experience       &  7.7\% &  3.3\% &  3.5\% \\
        TF-IDF overlap               &  5.5\% &  6.5\% &  5.2\% \\
        Experience freshness         &  1.3\% &  2.2\% &  1.3\% \\
        Experience delta             &  0.7\% &  0.6\% &  1.7\% \\
        Degree match       &  2.6\% &  0.3\% &  0.8\% \\
        MiniLM full-profile similarity &  0.4\% &  0.1\% &  0.0\% \\
        \bottomrule
    \end{tabular}
\end{table}
\subsection{Stage 2: Negative Strategy Selection}
\label{sec:exp_recruiter_negatives}
 
Having established XGBoost as the strongest classifier, the model type is now held fixed and the negative construction strategy is varied. Table~\ref{tab:recruiter_neg_comparison} reports XGBoost test set performance for each strategy, where each model is evaluated on a test set constructed with the same negative type as its training data.
 
\subsubsection{Synthetic vs.\ LLM negatives}
The most striking result is the gap between the synthetic and LLM models: AUC 0.852 vs.\ 0.724, a difference of 0.128. This gap is primarily a task-difficulty effect, not a quality difference. The synthetic model is tested on random non-applicants, who by construction are unlikely to match the job on any meaningful feature; almost any signal separates them from strong applicants. The LLM model is tested on poor-fit applicants from the real pool --- candidates who self-selected to apply despite a mismatch. These share the same self-selection signal as positives, forcing the model to rely on deeper feature differences. The lower AUC correctly reflects the genuine difficulty of that discrimination task.
 
\subsubsection{Combined as the best choice}
The combined model achieves AUC 0.755 on a mixed test set with equal proportions of LLM and synthetic negatives, outperforming the LLM-only model (0.724) but underperforming the synthetic-only model (0.852). Crucially, in the congestion balancing context of Chapter~\ref{chap:congestion_balancing}, the model must score candidates who may not have applied to the job at all --- which is precisely the synthetic scenario. A model trained exclusively on LLM negatives has never seen that setting and may not generalise well to it. The combined model handles both regimes and achieves the best verified performance across them.
 
Table~\ref{tab:recruiter_fi_strategies} shows XGBoost feature importances across all three training strategies, revealing a gradient rather than a simple redistribution. The PCA interaction components decrease steadily from LLM to synthetic (85.6\% $\rightarrow$ 81.4\%), but drop sharply in the combined setting (61.6\%) --- a reduction of 24\% relative to LLM-only. Qwen cosine similarity moves in the opposite direction: negligible at 1.9\% for LLM, modest at 3.8\% for synthetic, then jumping to 17.2\% in combined, becoming the second most important feature. TF-IDF overlap roughly doubles from LLM to synthetic (5.2\% $\rightarrow$ 9.3\%) and increases further in combined (10.3\%).
 
An interpretation is that the PCA components are specialised for hard within-pool discrimination, capturing subtle co-feature patterns that matter when positive and negative examples lie close together in feature space. When synthetic negatives enter the training mix, easier separations also need to be learned, and for these a scalar cosine similarity or keyword overlap is sufficient. The model converges on cosine similarity as a robust cross-regime signal.
 
Noteworthy is the emergence of \texttt{full\_profile\_cosine\_sim} (0.0\% $\rightarrow$ 3.2\% in combined). The LLM model assigns it zero importance --- within-pool fine-grained differences are better captured by the Qwen interaction. When non-applicants enter training, the MiniLM full-profile similarity provides a useful coarse-grained signal for filtering obvious mismatches, and the model begins to exploit it.
 
\begin{table}[H]
    \centering
    \caption{XGBoost feature importance across all three negative training strategies (last cross-validation fold). PCA components are aggregated into a single row.}
    \label{tab:recruiter_fi_strategies}
    \begin{tabular}{lrrr}
        \toprule
        \textbf{Feature} & \textbf{LLM only} & \textbf{Synthetic only} & \textbf{Combined} \\
        \midrule
        Interaction PCA (aggregated) & 85.6\% & 81.4\% & 61.6\% \\
        Qwen cosine similarity       &  1.9\% &  3.8\% & 17.2\% \\
        TF-IDF overlap               &  5.2\% &  9.3\% & 10.3\% \\
        Experience delta             &  1.7\% &  1.2\% &  4.0\% \\
        Full-profile cosine sim.     &  0.0\% &  1.3\% &  3.2\% \\
        Total years experience       &  3.5\% &  0.8\% &  2.2\% \\
        Degree match                 &  0.8\% &  1.3\% &  1.1\% \\
        Experience freshness         &  1.3\% &  1.0\% &  0.5\% \\
        \bottomrule
    \end{tabular}
\end{table}
 
\begin{table}[H]
    \centering
    \caption{Effect of negative sampling strategy on XGBoost test set performance. Each model is evaluated on a test set matching its training negatives; AUC values reflect both model quality and task difficulty and are not directly comparable across rows (see text).}
    \label{tab:recruiter_neg_comparison}
    \begin{tabular}{lrrrrr}
        \toprule
        \textbf{Negative Type} & \textbf{Accuracy} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} & \textbf{AUC} \\
        \midrule
        Synthetic (non-applicants)   & 0.772 & 0.818 & 0.748 & 0.781 & 0.852 \\
        LLM (Poor / Very Poor Fit)   & 0.669 & 0.675 & 0.600 & 0.636 & 0.724 \\
        Combined                     & 0.687 & 0.721 & 0.624 & 0.669 & 0.755 \\
        \bottomrule
    \end{tabular}
\end{table}
 
\subsubsection{Final Model}
\label{sec:exp_recruiter_final}
 
The two-stage selection process yields a clear conclusion: \textbf{XGBoost trained on combined negatives} is the final recruiter-side model. It achieves the best balance between the two extremes, generalises to both the within-applicant-pool ranking scenario and the broader candidate-scoring context in congestion balancing, and outperforms the LLM-only model on a verified test set. This model is serialised and used as the recruiter-side suitability scorer in Chapter~\ref{chap:congestion_balancing}.
 
\subsubsection{Qualitative Sanity Check}
\label{sec:exp_recruiter_sanity}
 
To validate that the final combined model behaves sensibly in practice, three jobs are sampled from Windows~5--6 — jobs the model has never seen during training. The three cases are chosen to probe different aspects of model behaviour: generalisation to non-applicants (Job~1), within-pool differentiation among actual applicants (Job~2), and mixed ranking across both groups (Job~3). All scores are produced by the final XGBoost model trained on combined negatives.
 
\paragraph{Job 257565 — Accounts Receivable Clerk: non-applicant generalisation.}
This job requires 1--2 years of business-to-business collections experience along with cash posting and basic Excel skills. Five random non-applicants are scored to test whether the model generalises to candidates it was not explicitly trained to rank (the combined model saw synthetic non-applicants during training, but not these specific individuals). Table~\ref{tab:sanity_ar_clerk} shows the results.
 
\begin{table}[H]
    \centering
    \caption{Sanity check — Job 257565 (Accounts Receivable Clerk). Five random non-applicants scored by the final combined model.}
    \label{tab:sanity_ar_clerk}
    \small
    \begin{tabular}{r r l r p{7cm}}
        \toprule
        \textbf{Rank} & \textbf{Score} & \textbf{Degree} & \textbf{Exp} & \textbf{Recent History} \\
        \midrule
        1 & 81.0\% & High School  & 10y & Customer Service, File Clerk, Cashier \\
        2 & 69.1\% & ---          &  9y & Accounting Asst / AP Specialist, Teller \\
        3 & 34.0\% & Bachelor's   &  7y & Licensed Personal Banker, Asst Manager \\
        4 & 31.1\% & Bachelor's   &  4y & IT Specialist, IT Support Technician, Help Desk \\
        5 & 24.9\% & High School  & 23y & Lead Security Coordinator, IT Operations \\
        \bottomrule
    \end{tabular}
\end{table}
 
The model correctly separates finance-adjacent profiles from unrelated backgrounds. IT and security candidates rank at the bottom (31\% and 25\%), while cash-handling and accounting profiles rank at the top. One nuance worth noting: the cashier/customer service profile (81\%) outranks the AP specialist (69\%) despite the AP specialist being closer to AR work. This is likely a TF-IDF keyword effect — terms such as \textit{collections} and \textit{cash posting} in the job description overlap more strongly with cashier terminology than with the more technical accounting vocabulary in the AP specialist's profile. This is a known limitation of keyword-based features rather than a failure of the model.
 
\paragraph{Job 377385 — Cash Counter: within-pool differentiation.}
This job requires cash handling, basic computer skills, and customer service experience. Five actual applicants are scored to test whether the model creates meaningful differentiation within the hard discrimination task — distinguishing better from worse fits among people who all chose to apply. Table~\ref{tab:sanity_cash_counter} shows the results.
 
\begin{table}[H]
    \centering
    \caption{Sanity check — Job 377385 (Cash Counter). Five actual applicants scored by the final combined model.}
    \label{tab:sanity_cash_counter}
    \small
    \begin{tabular}{r r l r p{7cm}}
        \toprule
        \textbf{Rank} & \textbf{Score} & \textbf{Degree} & \textbf{Exp} & \textbf{Recent History} \\
        \midrule
        1 & 43.3\% & High School &  35y & Route Account Manager, Refreshment Services, Admin \\
        2 & 37.2\% & High School &   7y & Clerical, Youth Activity Attendant, Customer Service \\
        3 & 30.9\% & High School &   2y & Housekeeping, Customer Service, Elevator Operator \\
        4 & 18.3\% & ---         &   3y & Customer Service Representative \\
        5 &  8.6\% & ---         &  13y & Asset Protection Supervisor, Loss Prevention Manager \\
        \bottomrule
    \end{tabular}
\end{table}
 
The model creates a meaningful score spread (8.6\%--43.3\%) among applicants who all applied for the role. The top candidate has 35 years of experience including direct cash-handling and administrative work — exactly the profile the job description targets. The bottom-ranked candidate is a loss prevention manager, whose background in surveillance and asset protection shares little with cash counting or customer-facing finance work. Absolute scores are lower here than in Job~1 because the model is operating in the harder within-pool regime, where it must rely on finer feature differences; the scores reflect fit quality rather than an absolute shortlist threshold.
 
\paragraph{Job 38902 — Automotive Service Manager: mixed ranking.}
This job requires 1--3 years of automotive service or customer service experience, leadership skills, and a valid driver's licence. Five applicants and five random non-applicants are scored together to probe whether the model ranks the two groups sensibly in a realistic mixed setting. Table~\ref{tab:sanity_auto_manager} shows the full combined ranking.
 
\begin{table}[H]
    \centering
    \caption{Sanity check — Job 38902 (Automotive Service Manager). Five applicants (A) and five non-applicants (N) scored by the final combined model.}
    \label{tab:sanity_auto_manager}
    \small
    \begin{tabular}{r r c l r p{6.5cm}}
        \toprule
        \textbf{Rank} & \textbf{Score} & \textbf{Type} & \textbf{Degree} & \textbf{Exp} & \textbf{Recent History} \\
        \midrule
        1  & 80.4\% & A & High School &  22y & VW Master Certified Service Manager, Service Manager \\
        2  & 67.0\% & A & Master's    &   9y & Transportation Security Officer, HR Assistant \\
        3  & 56.1\% & N & ---         &  17y & Customer Service Rep (Netflix), Closing Supervisor \\
        4  & 48.1\% & N & Bachelor's  &  26y & Tutor, Hardware Technician, Bank Teller \\
        5  & 47.9\% & A & Bachelor's  &   3y & Parking and Security Integrator, Intern \\
        6  & 47.6\% & N & ---         &   8y & Direct Support Staff, Customer Service \\
        7  & 41.0\% & A & Bachelor's  &   9y & Express Management, Retail Store Manager, Asst Manager \\
        8  & 40.3\% & N & Bachelor's  &   4y & Loan Officer, Claims Analyst \\
        9  & 25.3\% & N & ---         &   6y & Dispatcher, Luggage Agent \\
        10 & 19.4\% & A & ---         &  11y & Business Owner, In-charge Operations \\
        \bottomrule
    \end{tabular}
\end{table}
 
The top candidate — a Volkswagen Master Certified Service Manager with 22 years in automotive service — is correctly identified by a wide margin (80.4\%). This is the clearest possible true positive: the candidate's entire history is a direct match for the role. Applicants score 51.1\% on average versus 43.5\% for non-applicants, confirming that the model correctly assigns higher suitability to the self-selected group overall.
 
The interleaving of non-applicants in ranks 3--4 and 6 is expected rather than a failure. The Netflix customer service supervisor (rank~3, 56.1\%) and the hardware technician/bank teller (rank~4, 48.1\%) have varied backgrounds with supervisory and customer-facing elements that are genuinely adjacent to an automotive service manager role. The model is not making an error — it is correctly recognising partial feature overlap. The dispatcher and luggage agent (rank~9, 25.3\%) and loan officer (rank~8, 40.3\%) score lower, as their backgrounds share less with automotive service work. This middle-range interleaving is precisely what a combined model trained on both hard and easy negatives is expected to produce: it does not hard-separate applicants from non-applicants, but scores all candidates on genuine fit quality.
 
% ============================================================
\section{Congestion Balancing Experiments}
\label{sec:exp_congestion}
% ============================================================
 
\subsection{Experiment Goal}
\label{sec:exp_congestion_goal}
 
The congestion balancing experiments pursue three goals in sequence. The first
is to demonstrate that both forms of congestion described in
Chapter~\ref{chap:congestion_balancing} are genuinely present in the data,
before any intervention is applied and to form a baseline for the subsequent experiments. The second is to show that the job-side and
user-side market-maker strategies each independently reduce their respective
form of congestion while preserving acceptable match quality. The third is toshow that the dual-sided hybrid pipeline from
Section~\ref{sec:hybrid_strategy} addresses both forms simultaneously, and
that the bilateral scoring alone — without explicit congestion penalties —
already produces a measurable improvement compared to the baseline results.
 
\subsection{Experiment Setting}
\label{sec:exp_congestion_setting}
\paragraph{Simulation arena.}
All congestion experiments are conducted on the Window~7 application pool,
which serves as the held-out test set (Section~\ref{sec:custom_split}). Users who submitted more than 45 applications in Window~7 are removedbefore the simulation begins, as their behaviour is not representative of
genuine job seeking and would distort the congestion metrics. They are considered as outliers and this is done for simplicity.

 
\paragraph{Threshold derivation.}
A key design decision is that the zone thresholds $\tau_{\min}$ and
$\tau_{\max}$ — which define the boundary between starved, safe, and congested
entities — are derived exclusively from the historical Windows~1--6 data, not
from Window~7. This mirrors realistic deployment conditions: in practice, a
platform can only observe past behaviour when configuring its market-maker, and
must apply the resulting thresholds to future traffic without knowing the future
distribution in advance. The thresholds are therefore not optimised against any
validation objective; they are a principled reading of what the historical
market looked like, translated into concrete zone boundaries.
 
For the job side, the lower threshold $\tau_{\min}^{\text{job}} = 2$ is set to
the median number of applications per job in Windows~1--6, and the upper
threshold $\tau_{\max}^{\text{job}} = 9$ is set to the 90th percentile. This
means the safe zone captures what a typical job in the historical market
received, while jobs above the 90th percentile are flagged as congested. For
the candidate side, the thresholds $\tau_{\min}^{\text{cand}} = 1$ and
$\tau_{\max}^{\text{cand}} = 5$ are set to reflect the recruiter's screen
capacity of top-5: a candidate who has never been shortlisted is starved, and a candidate who has been shortlisted more than five times is accumulating more
attention than a single recruiter screens per job.
 
\paragraph{Parameter values.}
Tables~\ref{tab:job_mm_params} and~\ref{tab:cand_mm_params} list the
market-maker parameter values used in all congestion experiments. As with the
thresholds, these are design choices rather than optimised hyperparameters:
the boost and penalty magnitudes are set to be on the same $[0,1]$ probability
scale as the base scores they modify, and the shape parameters ($\gamma_p$,
$\kappa_p$, $k$) are chosen so that each functional form reaches its maximum
penalty at approximately the hard cap. Figure~\ref{fig:modification_functions} illustrates the three functional
forms over the congestion range. The sigmoid curve reaches its maximum
penalty earliest, making it the most aggressive redistributor for moderately
congested jobs. The quadratic curve is initially lenient but accelerates
once congestion becomes severe. The linear curve descends at a constant
rate and reaches its maximum only at the hard cap.

\begin{table}[htbp]
    \centering
    \caption{Market-maker parameter values for job-side congestion mitigation.
    Base scores are sigmoid-transformed Two-Tower logits in $[0,1]$, so the
    boost and penalty magnitudes are on the same probability scale.}
    \label{tab:job_mm_params}
    \begin{tabular}{@{}llp{7.5cm}@{}}
        \toprule
        \textbf{Parameter} & \textbf{Value} & \textbf{Description} \\
        \midrule
        $\tau_{\min}^{\text{job}}$ & 2    & Starved threshold (applications) \\
        $\tau_{\max}^{\text{job}}$ & 9   & Congested threshold (applications) \\
        $H^{\text{job}}$           & 50   & Hard cap; job removed from rankings \\
        $B$                        & 0.25 & Maximum boost magnitude \\
        $P$                        & 0.25 & Maximum penalty magnitude \\
        $\gamma_b$ (linear)        & 0.11  & Boost rate per application below $\tau_{\min}$ \\
        $\gamma_p$ (linear)        & 0.006 & Penalty rate per application beyond $\tau_{\max}$ \\
        $\kappa_b$ (quadratic)     & 0.06  & Quadratic boost scaling parameter \\
        $\kappa_p$ (quadratic)     & 0.001 & Quadratic penalty scaling parameter \\
        $k_b$ (sigmoid)            & 1.4   & Sigmoid steepness (boost side) \\
        $k_p$ (sigmoid)            & 0.25  & Sigmoid steepness (penalty side) \\
        \bottomrule
    \end{tabular}
\end{table}
 
\begin{table}[htbp]
    \centering
    \caption{Market-maker parameter values for user-side congestion mitigation.
    Base scores are XGBoost shortlisting probabilities in $[0,1]$.}
    \label{tab:cand_mm_params}
    \begin{tabular}{@{}llp{7.5cm}@{}}
        \toprule
        \textbf{Parameter} & \textbf{Value} & \textbf{Description} \\
        \midrule
        $\tau_{\min}^{\text{cand}}$ & 2    & Starved threshold (shortlists) \\
        $\tau_{\max}^{\text{cand}}$ & 6    & Congested threshold (shortlists) \\
        $H^{\text{cand}}$           & 10   & Hard cap; candidate removed from shortlisting \\
        $B$                         & 0.25 & Maximum boost magnitude \\
        $P$                         & 0.25 & Maximum penalty magnitude \\
        
        $\gamma_p$ (linear)         & 0.083 & Penalty rate per shortlist beyond $\tau_{\max}$ \\
        $\kappa_p$ (quadratic)      & 0.028 & Quadratic penalty scaling parameter \\
        $k$ (sigmoid)               & 1.0 & Sigmoid steepness \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{figure}
    \centering
    \includegraphics[width=\textwidth]{images/modification_functions.png}
    \caption{The three modification functions $\delta(c)$ plotted over the
    application count range $[0, H^{\text{job}}]$. Zone boundaries at
    $\tau_{\min}=2$ and $\tau_{\max}=9$ are shown as dashed vertical lines.}
    \label{fig:modification_functions}
\end{figure}

\paragraph{Simulation mechanics.}
For the job-side and hybrid experiments, Two-Tower recommendations and
recruiter scores are pre-computed and cached before any strategy is run, so
that all four strategy variants see identical model outputs and differ only in
how those outputs are combined and modified. Users and job inboxes are processed
sequentially; the running counters $c_j^{(t)}$ and $v_u^{(t)}$ are reset
between strategy runs. The weighting parameter $\alpha = 0.5$ is used in
all hybrid experiments for simplicity, giving equal weight to the Two-Tower and recruiter signals.
 
\paragraph{Metrics.}
Congestion is measured using the Gini coefficient $G$ (lower is more equal)
and the percentage of entities in each zone (starved, safe, congested) as
defined in Section~\ref{sec:prelim_zones}. To assess whether congestion
reduction comes at the cost of match quality, the average Two-Tower probability
$\bar{p}_{\text{TT}}$ and average recruiter probability $\bar{p}_{\text{rec}}$
are also reported across all applied (user, job) pairs. A strategy that reduces
congestion while keeping these averages close to the free-market baseline is
considered successful.
 
\subsection{Job-Side Congestion}
\label{sec:exp_job_congestion}
% ============================================================

\subsubsection{Historical Analysis}
\label{sec:exp_job_congestion_baseline}
Figure~\ref{fig:job_congestion_baseline} shows the distribution of applications
per job in the Windows~1--6 historical pool. The distribution is heavily
right-skewed: the vast majority of jobs receive zero or one application, while
a long tail of popular postings accumulates a disproportionate share of
candidate attention. Table~\ref{tab:job_congestion_baseline} quantifies this
across both the historical period and the held-out Window~7, confirming that
the pattern is stable across time. The
Gini coefficients of 0.8655 and 0.8575 for Windows~1--6 and Window~7
respectively indicate a high and consistent level of inequality. In both
periods, roughly 80\% of jobs are starved — receiving fewer than two
applications — while only around 15\% sit in the safe zone. This stability
justifies using the Windows~1--6 distribution to derive the zone thresholds
applied to Window~7.
 
\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/job_application_distribution_window_1_6.png}
    \caption{Distribution of applications per job in the Windows~1--6 historical
    pool. The vertical dashed lines indicate the starved/safe (green) and safe/congested (orange) thresholds}
    \label{fig:job_congestion_baseline}
\end{figure}
 
\begin{table}[H]
    \centering
    \caption{Job-side congestion across historical and test periods.
    Thresholds are derived from the Windows~1--6 distribution and applied
    consistently across both rows.}
    \label{tab:job_congestion_baseline}
    \begin{tabular}{@{}lrrrr@{}}
        \toprule
        \textbf{Period} & \textbf{Gini} & \textbf{Starved ($<$2)} & \textbf{Safe (2--9)} & \textbf{Congested ($>$9)} \\
        \midrule
        Historical Windows~1--6 & 0.8655 & 82.6\% & 14.7\% & 2.8\% \\
        Historical Window~7     & 0.8575 & 80.9\% & 15.8\% & 3.3\% \\
        \bottomrule
    \end{tabular}
\end{table}
 
\subsubsection{Market-maker simulation results}
Table~\ref{tab:job_mm_results} compares all strategies on Window~7. The first
notable finding is that the free-market Two-Tower recommender makes congestion
\emph{worse} than the historical baseline: the Gini rises from 0.8575 to
0.9625 and the starved fraction increases from 80.9\% to 90.8\%. This is
expected — the Two-Tower model optimises for relevance and will confidently
direct many users towards the same popular jobs, amplifying the feedback loop
rather than correcting it. This result motivates the market-maker intervention
directly: an uncorrected recommender is not a neutral system; it actively
concentrates traffic further.
 
All three market-maker strategies produce a large improvement over the free
market. The Gini drops from 0.9625 to between 0.5985 and 0.6131 depending on
the strategy, and the safe zone grows from 6.4\% to between 22.7\% and 39.2\%.
 
The three functional forms produce meaningfully different distributions, which
is the intended effect of adjusting the penalty scales. The sigmoid strategy
achieves the best overall balance: it has the lowest Gini (0.5985), the
smallest starved fraction (57.8\%), and the largest safe zone (39.2\%). The
quadratic strategy achieves a lower Gini than linear (0.6016 vs 0.6131) but
leaves more jobs starved (74.5\% vs 64.8\%). This is explained by its design:
the quadratic penalty is lenient on mildly congested jobs but becomes
aggressive on heavily congested ones, pulling down the extreme tail while
doing less to help the mildly under-applied. The linear strategy is the most
conservative, applying a steady uniform penalty that produces the least
redistribution but preserves the highest match quality (TT probability 0.8999).
 
The match quality cost is modest across all strategies. The average Two-Tower
probability drops from 0.9537 under free market to between 0.8928 and 0.8999
— a reduction of 5--6\%. Crucially, the average recruiter probability is
almost unchanged (0.5711 free market vs 0.5684--0.5700 with penalties),
indicating that from the recruiter's perspective the suitability of applied
candidates is preserved despite the redistribution. The market-maker is
redirecting traffic toward genuinely qualified but overlooked candidates, not
toward random jobs.
 
\begin{table}[H]
    \centering
    \caption{Job-side market-maker results on Window~7. Historical W7 shows
    the raw application distribution for reference. $\bar{p}_{\text{TT}}$ is
    the average Two-Tower probability of applied (user, job) pairs;
    $\bar{p}_{\text{rec}}$ is the average recruiter suitability probability.
    Both serve as proxies for match quality.}
    \label{tab:job_mm_results}
    \begin{tabular}{@{}lccccrr@{}}
        \toprule
        \textbf{Strategy} & \textbf{Gini} & \textbf{Starved} & \textbf{Safe} & \textbf{Congested} & $\bar{p}_{\textbf{TT}}$ & $\bar{p}_{\textbf{rec}}$ \\
        \midrule
        Historical W7         & 0.8575 & 80.9\% & 15.8\% & 3.3\% & ---    & ---    \\
        Free Market (TT only) & 0.9625 & 90.8\% &  6.4\% & 2.9\% & 0.9537 & 0.5711 \\
        \midrule
        Linear                & 0.6131 & 64.8\% & 32.7\% & 2.5\% & 0.8999 & 0.5700 \\
        Quadratic             & 0.6016 & 74.5\% & 22.7\% & 2.8\% & 0.8954 & 0.5699 \\
        Sigmoid               & 0.5985 & 57.8\% & 39.2\% & 3.0\% & 0.8928 & 0.5684 \\
        \bottomrule
    \end{tabular}
\end{table}
 
Figure~\ref{fig:job_mm_grid} visualises the application distributions produced
by each strategy. The free-market panel confirms the pathological concentration
described above: a massive spike at zero and a flat, heavy tail extending to
the hard cap. All three market-maker panels shift mass rightward out of the
starved zone and truncate the tail earlier, but do so in visibly different
ways. The linear strategy spreads traffic broadly, producing a gradual decay
across the safe zone. The quadratic strategy concentrates the redistribution
more sharply: the safe zone fills unevenly, with a secondary bump around
20--24 applications where jobs cluster just below the point at which the
quadratic penalty becomes severe. The sigmoid panel shows the smoothest
redistribution, with a steady decline through the safe zone and the earliest
tail truncation of the three, consistent with its lowest Gini and largest safe
fraction.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/all_universes_job_congestion_grid.png}
    \caption{Application distributions per job for all four algorithmic
    universes on Window~7. The red and orange dashed lines mark the
    starved/safe and safe/congested thresholds respectively. The hard cap
    at $H=50$ is never visibly triggered as a cliff; the gradient penalties
    suppress traffic before any job reaches it.}
    \label{fig:job_mm_grid}
\end{figure}
\subsubsection{Qualitative Inference Evaluation}
\label{sec:exp_job_congestion_sanity}
To verify that the aggregate improvements translate to sensible individual
recommendations, the same two users from Section~\ref{sec:exp_recruiter_sanity}
are examined across all four universes. Their profiles are summarised in
Tables~\ref{tab:user668904_profile} and~\ref{tab:user831532_profile}. 
 
\textbf{User 668904 — strong career signal.} In the historical universe this
user has a single entry (the one job they applied to), since the historical
pool contains only real applications. The free-market Two-Tower recommendations
are tightly clustered around accounts payable and accounting roles in Houston,
TX — consistent with 18 years of finance experience. Across the three
market-maker strategies, the geographic coherence to Houston is preserved in
all cases and the recommendations remain within the finance cluster. The
strategies redirect the user away from the most popular AP postings toward
slightly less applied-to but equally relevant alternatives: Tax Accountant,
Financial Accountant, and Accounting Analyst variants appear in place of the
exact same Senior AP Specialist roles that dominate the free market list.
The quadratic strategy surfaces two lower-scoring entries at ranks 1--2
(Document Controller and Energy Quantitative Analyst with original scores of
1.72 and 1.49 respectively) because those jobs had almost no applications and
received the largest boost. These are less obvious but not unreasonable for a
candidate with document control experience.
 
\textbf{User 831532 — cold-start user.} This user has no job history and only
2 years of experience, making the TT model rely entirely on location and basic
profile features. The free-market recommendations are geographically coherent
across the PA/NJ/DE tri-state area and semantically consistent around
community care and support roles (Discussed in Section~\ref{sec:exp_recruiter_sanity}). The market-maker strategies introduce more diversity
but with a visible cost. The linear strategy adds Home Health Aides (Langhorne
PA) at rank~2 — a reasonable care-adjacent role — but also surfaces HVAC
(Wilmington DE) at rank~8, which is semantically distant. The sigmoid strategy
similarly surfaces a Water Operator role, and the quadratic strategy places
Loss Prevention (Middletown NJ, original score 1.05) at rank~1 purely due to
its low application count.
 
The contrast between the two users reveals the market-maker's inherent
tradeoff. When the Two-Tower model has high confidence — a clear career signal
and many relevant jobs in the pool — the penalty only redirects the user
toward equally relevant alternatives. When model confidence is lower — a
cold-start user with minimal profile signal — boosted low-application jobs
occasionally surface that are less semantically coherent. This is consistent
with the aggregate match quality drop observed in Table~\ref{tab:job_mm_results}:
the 5--6\% reduction in average TT probability is not uniformly distributed
across users, but is concentrated among those for whom the base model is less
certain. The market-maker optimises for distributional fairness at the market
level, not for individual relevance, and this distinction is most visible at
the user level for cold-start profiles. Tables \ref{tab:sanity_668904} and~\ref{tab:sanity_831532} show the top-3 recommendations for each user across all universes, along with their original TT probabilities for reference. For a full list of all 10 recommendations per user per universe, see Appendix~\ref{app:sanity_check_full}.
\begin{table}[H]
    \centering
    \caption{Top-3 recommendations for User~668904 across all universes.
    New Score is the modified ranking score; Orig Score is the raw Two-Tower
    logit.}
    \label{tab:sanity_668904}
    \begin{tabular}{@{}clp{6.5cm}l@{}}
        \toprule
        \textbf{Rank} & \textbf{New Prob} & \textbf{Job Title} & \textbf{Orig Prob} \\
        \midrule
        \multicolumn{4}{l}{\textit{Free Market}} \\
        1 & 0.729 & Senior Accounts Payable Specialist (Houston, TX) & 0.989 \\
        2 & 0.729 & Accounting Payable / Receivable Specialist (Houston, TX) & 0.989 \\
        3 & 0.729 & Accounting Assistant (Houston, TX) & 0.988 \\
        \midrule
        \multicolumn{4}{l}{\textit{Market Maker Linear}} \\
        1 & 0.748 & Tax Accountant (Houston, TX) & 0.977 \\
        2 & 0.747 & Financial Accountant (Houston, TX) & 0.975 \\
        3 & 0.747 & Junior Accountant (Houston, TX) & 0.974 \\
        \midrule
        \multicolumn{4}{l}{\textit{Market Maker Quadratic}} \\
        1 & 0.748 & Document Controller 4 (Houston, TX) & 0.848 \\
        2 & 0.742 & Energy Quantitative Analyst (Houston, TX) & 0.817 \\
        3 & 0.739 & Accounts Payable Supervisor (Houston, TX) & 0.979 \\
        \midrule
        \multicolumn{4}{l}{\textit{Market Maker Sigmoid}} \\
        1 & 0.756 & Tax Accountant (Houston, TX) & 0.977 \\
        2 & 0.755 & Accounting Analyst — Tubular Accounting (Houston, TX) & 0.973 \\
        3 & 0.754 & Reporting/GAAP Accountant (Houston, TX) & 0.967 \\
        \bottomrule
    \end{tabular}
\end{table}
 
\begin{table}[H]
    \centering
    \caption{Top-3 recommendations for User~831532 across all universes.
    $\dagger$~Indicates a job the user actually applied to in Window~7.}
    \label{tab:sanity_831532}
    \begin{tabular}{@{}clp{6.5cm}l@{}}
        \toprule
        \textbf{Rank} & \textbf{New Prob} & \textbf{Job Title} & \textbf{Orig Prob} \\
        \midrule
        \multicolumn{4}{l}{\textit{Free Market}} \\
        1 & 0.728 & Customer Service Representative (Broomall, PA) & 0.985 \\
        2 & 0.728 & Nanny / Childcare / Teacher (Madison, NJ) & 0.983 \\
        7 & 0.726 & Residential Youth Care Workers Needed$^\dagger$ (Conshohocken, PA) & 0.973 \\
        \midrule
        \multicolumn{4}{l}{\textit{Market Maker Linear}} \\
        1 & 0.726 & Shelter Staff (Wilmington, DE) & 0.975 \\
        2 & 0.726 & Home Health Aides (Langhorne, PA) & 0.865 \\
        3 & 0.726 & Residential Youth Care Workers Needed$^\dagger$ (Conshohocken, PA) & 0.973 \\
        \midrule
        \multicolumn{4}{l}{\textit{Market Maker Quadratic}} \\
        1 & 0.728 & Loss Prevention openings (Middletown, NJ) & 0.742 \\
        2 & 0.726 & Shelter Staff (Wilmington, DE) & 0.975 \\
        7 & 0.725 & Nanny / Childcare / Teacher (Madison, NJ) & 0.983 \\
        \midrule
        \multicolumn{4}{l}{\textit{Market Maker Sigmoid}} \\
        1 & 0.730 & CHHA — Home Health Aide (Vineland, NJ) & 0.843 \\
        2 & 0.729 & Certified Home Health Aides (Medford, NJ) & 0.836 \\
        6 & 0.726 & Residential Youth Care Workers Needed$^\dagger$ (Conshohocken, PA) & 0.973 \\
        \bottomrule
    \end{tabular}
\end{table}
\subsection{User-Side Congestion Analysis}
\label{sec:exp_user_congestion}
\subsubsection{Historical Analysis}
Figure~\ref{fig:user_congestion_baseline} shows the distribution of recruiter
shortlists per candidate in the Windows~1--6 historical pool, obtained by
simulating the top-5 recruiter screen on the raw application data. The
distribution is heavily right-skewed: a small fraction of candidates
accumulates a disproportionate share of recruiter attention while roughly
half receive fewer than two shortlists. Unlike the job-side distribution
however, the tail is considerably less extreme — candidates who applied to
at least one job have a non-trivial chance of being shortlisted, so the
spike at zero is smaller. Table~\ref{tab:user_congestion_baseline}
quantifies this across both periods.

The Gini coefficients of 0.5356 and 0.5370 for Windows~1--6 and Window~7
respectively confirm two things. First, meaningful inequality exists:
recruiter attention is far from uniformly distributed. Second, the pattern
is highly stable across time — a delta of only 0.0014 between periods. In
both windows, roughly half of all candidates are starved, receiving fewer
than two shortlists, while around 41--43\% sit in the safe zone. This
stability justifies using the Windows~1--6 distribution to derive the zone
thresholds applied to Window~7.

It is worth noting that both the historical and the market-maker baselines
in this section are derived from the real application pool — the recruiter
model is applied to the actual job inboxes, with no Two-Tower recommendation
step involved. This is a deliberate design choice for the standalone
user-side analysis: the market-maker operates purely on the recruiter
ranking stage, isolating its effect from any upstream recommendation
behaviour. The full interaction between the Two-Tower and the recruiter
— where TT-generated applications form the inboxes the recruiter then
ranks — is evaluated separately in the hybrid experiment
(Section~\ref{sec:exp_hybrid_results}).

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{images/views_per_candidate_past_windows_1_6.png}
    \caption{Distribution of recruiter shortlists per candidate in the
    Windows~1--6 historical pool, obtained by simulating the top-5
    recruiter screen on the raw application data. The vertical dashed lines
    indicate the starved/safe ($\tau_{\min}^{\text{cand}} = 2$) and
    safe/congested ($\tau_{\max}^{\text{cand}} = 6$) thresholds.}
    \label{fig:user_congestion_baseline}
\end{figure}

\begin{table}[htbp]
    \centering
    \caption{User-side congestion across historical and test periods,
    measured by applying the top-5 recruiter screen to the raw application
    pool. Thresholds are derived from the Windows~1--6 distribution and
    applied consistently across both rows.}
    \label{tab:user_congestion_baseline}
    \begin{tabular}{@{}lrrrr@{}}
        \toprule
        \textbf{Period} & \textbf{Gini} & \textbf{Starved ($<$2)} &
        \textbf{Safe (2--6)} & \textbf{Congested ($>$6)} \\
        \midrule
        Historical Windows~1--6 & 0.5356 & 48.7\% & 42.6\% & 8.7\% \\
        Historical Window~7     & 0.5370 & 52.3\% & 40.8\% & 6.9\% \\
        \bottomrule
    \end{tabular}
\end{table}

\subsubsection{Market-maker simulation results}
Table~\ref{tab:user_mm_results} compares the three market-maker strategies
against the Window~7 historical baseline. The baseline here is the recruiter
screen applied to the real Window~7 application pool — identical to the
Historical Window~7 row above — so no separate free-market recommender
row is included. The market-maker operates purely at the recruiter ranking
stage: for each job inbox, candidates are re-ranked by their recruiter score
modified by a congestion penalty based on how many times they have already
been shortlisted in the current simulation run.

All three strategies produce a consistent improvement over the baseline.
The Gini drops from 0.5370 to approximately 0.481 and the safe zone grows
from 40.8\% to around 46\%. The match quality cost is minimal: the average
base recruiter score drops from 0.8576 to approximately 0.847 — a reduction
of around 1.2\% — indicating that the market-maker is redistributing
shortlisting attention toward genuinely suitable but overlooked candidates
rather than arbitrary ones.

The three functional forms produce near-identical outcomes: Gini values of
0.4809, 0.4816, and 0.4807 for linear, quadratic, and sigmoid respectively
— a spread of less than 0.001. The overlay in
Figure~\ref{fig:user_mm_grid} confirms this visually: the three curves are
almost perfectly superimposed. This contrasts sharply with the job side,
where sigmoid clearly outperformed the others. The convergence here is
structural rather than coincidental: because the congested zone spans only
three integer values (views 7, 8, and 9) before the hard cap at
$H^{\text{cand}} = 10$, the functional form of the penalty has negligible
influence on the outcome — all three strategies apply a similar suppression
within that narrow range. This asymmetry between the job and candidate sides
reflects a fundamental difference in the nature of the two congestion
problems: job-side congestion is driven by a recommendation feedback loop
that creates a wide, heavy tail the penalty curves can act across, while
candidate-side congestion is structurally bounded by individual application
behaviour and cannot develop the same runaway dynamic.

\begin{table}[htbp]
    \centering
    \caption{User-side market-maker results on Window~7. The baseline is
    the top-5 recruiter screen applied to the raw application pool, with
    no market-maker intervention. $\bar{p}_{\text{rec}}$ is the average
    base recruiter suitability probability of shortlisted (candidate, job)
    pairs, serving as a proxy for match quality.}
    \label{tab:user_mm_results}
    \begin{tabular}{@{}lccccr@{}}
        \toprule
        \textbf{Strategy} & \textbf{Gini} & \textbf{Starved} &
        \textbf{Safe} & \textbf{Congested} & $\bar{p}_{\text{rec}}$ \\
        \midrule
        Baseline (raw apps)   & 0.5370 & 52.3\% & 40.8\% & 6.9\% & 0.8576 \\
        \midrule
        Linear                & 0.4809 & 47.3\% & 46.4\% & 6.3\% & 0.8470 \\
        Quadratic             & 0.4816 & 47.4\% & 46.3\% & 6.3\% & 0.8471 \\
        Sigmoid               & 0.4807 & 47.3\% & 46.3\% & 6.3\% & 0.8469 \\
        \bottomrule
    \end{tabular}
\end{table}

Figure~\ref{fig:user_mm_grid} visualises the shortlist distributions
produced by each strategy. All three market-maker panels shift mass
rightward out of the starved zone and truncate the tail earlier relative
to the baseline, but because the strategies are nearly indistinguishable
in their penalty curves at this scale, the panels differ only in the
magnitude of the starved spike at zero and one, not in the shape of the
tail. The hard cap at $H^{\text{cand}} = 10$ is never visibly triggered
as a cliff for the same reason as the job side: the gradient penalties
suppress shortlisting before any candidate reaches it.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{images/all_universes_user_congestion_grid.png}
    \caption{Shortlist distributions per candidate for the three
    market-maker strategies on Window~7, alongside the raw-application
    baseline. The red and orange dashed lines mark the starved/safe
    ($\tau_{\min}^{\text{cand}} = 2$) and safe/congested
    ($\tau_{\max}^{\text{cand}} = 6$) thresholds respectively. Unlike the
    job-side grid, the three market-maker panels are visually
    near-identical, reflecting the structural convergence of the penalty
    functions over the narrow congested zone.}
    \label{fig:user_mm_grid}
\end{figure}

\subsection{Hybrid Balancing Results}
\label{sec:exp_hybrid_results}

As discussed in Section~\ref{sec:hybrid_strategy}, the hybrid experiment evaluates the full dual-sided pipeline, where the
Two-Tower recommender and the recruiter model operate jointly across two
phases. In the first phase, users are scored against jobs using a combined score that sums the logit outputs of both models. The weighting parameter $\alpha = 0.5$ is used for simplicity, giving
equal weight to both signals. The Two-Tower term has an optional penalty term. 

In Phase~2, each job's inbox — formed by
the applications from Phase~1 — is ranked by the recruiter model, optionally
modified by a candidate-side congestion penalty $\delta_{\text{cand}}(v_u)$
based on how many times candidate $u$ has already been shortlisted. The
top-5 candidates per job are shortlisted.

Four universes are evaluated in an ablation design, using the sigmoid
functional form throughout since it produced the best results in the
standalone job-side experiments
(Section~\ref{sec:exp_job_congestion}):

\begin{enumerate}
    \item \textbf{Bilateral scoring, no penalties} — the combined
    $\alpha$-weighted score drives applications; the recruiter ranks
    inboxes without any congestion penalty. This isolates the effect of
    bilateral scoring alone.
    \item \textbf{Job-side sigmoid only} — the job congestion penalty
    $\delta_{\text{job}}$ is active in Phase~1; no candidate penalty in
    Phase~2.
    \item \textbf{Candidate-side sigmoid only} — no job penalty in
    Phase~1; the candidate congestion penalty $\delta_{\text{cand}}$ is
    active in Phase~2.
    \item \textbf{Both sigmoid} — both penalties active simultaneously.
    This is the complete dual-sided market-maker.
\end{enumerate}

\subsubsection{Bilateral scoring baseline}
Table~\ref{tab:hybrid_job_results} shows that bilateral scoring alone
already improves over the pure Two-Tower free market from
Section~\ref{sec:exp_job_congestion}: the job-side Gini drops from
0.9625 to 0.9182, and the starved fraction decreases from 90.8\% to
85.6\%. This improvement requires no explicit congestion penalty —
it arises purely from incorporating the recruiter signal into the
application ranking. Because the recruiter score captures candidate-job
fit from a different perspective than the Two-Tower, the combined score
diversifies which jobs users apply to, partially breaking the feedback
loop that concentrates traffic on popular postings.

The match quality picture is particularly striking. Compared to the
standalone Two-Tower free market ($\bar{p}_{\text{TT}} = 0.9537$), the
bilateral baseline shows a modest drop in average TT probability to
0.8966 (Table~\ref{tab:hybrid_scores}) — a reduction of 5.9\%. However, the average recruiter
probability rises significantly from 0.5711 to 0.8405. This is not a
contradiction: the TT model was optimised purely for relevance and
its top recommendations are jobs the user is likely to apply to, but
not necessarily jobs where the recruiter would consider them a strong
fit. By weighting the recruiter signal equally alongside the TT score,
the combined ranking steers users toward jobs where they are both
interested and genuinely suitable — accepting a small reduction in
application likelihood in exchange for a large gain in candidate-job
fit quality from the recruiter's perspective.

On the candidate side (Table~\ref{tab:hybrid_cand_results}), the
bilateral baseline produces a Gini of 0.6326 with 32.1\% of candidates
starved. This is considerably worse than the standalone user-side
baseline (Gini 0.5370, 52.3\% starved) because the population here
is different: the standalone simulation operated on the real application
pool where candidates self-selected into jobs. Here, the TT recommender
directs users toward jobs the model believes are relevant, which
concentrates applications — and therefore recruiter attention — more
unevenly than organic behaviour.

\subsubsection{Ablation: isolating each penalty}

\paragraph{Job-side penalty only.}
Adding the job-side sigmoid penalty reduces the job Gini substantially
from 0.9182 to 0.7276, and grows the safe zone from 11.1\% to 29.4\%.
However, this improvement is considerably smaller than in the standalone
job-side experiment, where the same sigmoid penalty achieved a Gini of
0.5985. The gap is explained by the structure of the combined scoring
function (Equation~\ref{eq:hybrid_score}): the job penalty
$\delta_{\text{job}}$ only acts on the Two-Tower component, which
carries weight $\alpha = 0.5$. The recruiter probability term is
congestion-unaware and pulls the combined score in the opposite
direction — a user may still apply to a congested job if the recruiter
score is high enough to outweigh the penalty. This dilution is a
fundamental consequence of the bilateral design: the recruiter signal
improves match quality but attenuates the market-maker's redistribution
power. A higher $\alpha$ would strengthen the penalty at the cost of
recruiter influence, and represents a tunable trade-off left for future
work.

Notably, despite the redistribution cost, the recruiter probability
remains substantially higher than the standalone Two-Tower free market
baseline (Table~\ref{tab:hybrid_scores}), confirming that the
bilateral scoring gain in candidate-job fit quality is preserved even
after the job penalty is applied. The TT probability is lower than the
standalone baseline (0.8671 vs 0.9537), which is expected — the
bilateral score never optimises for TT probability alone, and the job
penalty pushes this further by redirecting users away from their
highest-ranked jobs.

The candidate-side effect of the job penalty is striking: starved
candidates drop from 32.1\% to just 6.8\%, and the candidate Gini
falls from 0.6326 to 0.5256. This improvement is entirely indirect —
by redistributing applications more evenly across jobs, each job inbox
contains a more diverse set of candidates, so recruiter attention
naturally spreads. The trade-off is a rise in congested candidates from
6.7\% to 13.7\%: when the job penalty pushes applications toward
previously empty jobs, those jobs now have small inboxes where all
applicants are automatically shortlisted, including candidates who are
already frequently shortlisted elsewhere.

\paragraph{Candidate-side penalty only.}
The candidate-side penalty applied without any job penalty produces a
modest improvement on the candidate side: Gini drops from 0.6326 to
0.5884 and starved candidates from 32.1\% to 27.9\%. The job-side
distribution is unchanged from the bilateral baseline by design — the
candidate penalty operates only in Phase~2 and has no influence on
which jobs users apply to in Phase~1. The match quality scores are the also equal for the same reason. The candidate-side improvement
here is smaller than the job-side penalty's indirect effect (Gini 0.5884
vs 0.5256), confirming that redistributing the application pool in
Phase~1 is a more powerful lever for candidate equity than re-ranking
within fixed inboxes in Phase~2.

\subsubsection{Full dual-sided market-maker}
Universe~4 combines both penalties and achieves the best results across
all metrics. On the job side, the Gini and zone fractions are identical
to universe~2 (0.7276, 29.4\% safe) — as expected, since the candidate
penalty operates in Phase~2 and cannot influence Phase~1 job
distributions. On the candidate side, the full system achieves a Gini
of 0.4950 with only 3.9\% of candidates starved and 82.9\% in the safe
zone. Compared to the bilateral baseline this represents a reduction of
28.2\% in the starved fraction.

The congested fraction remains elevated at 13.2\%, marginally below the
13.7\% of universe~2. The candidate penalty suppresses repeat shortlisting
but cannot fully counteract the structural effect of the job penalty
pushing applications into small inboxes. This is an inherent tension in
the dual-sided design: the mechanisms that most effectively reduce
candidate starvation also create pathways for mild candidate congestion.

Match quality scores are identical to universe~2
(Table~\ref{tab:hybrid_scores}),
as expected — the candidate-side penalty operates exclusively in
Phase~2 and has no influence on which jobs users apply to in Phase~1.
The match quality cost of the full system is therefore entirely
attributable to the job-side penalty, not to the candidate-side
penalty. These reductions reflect the intended behaviour: the market-maker redirects some users away from
their highest-scoring jobs toward less popular alternatives, accepting
a modest match quality cost in exchange for a substantially more
equitable distribution of both applications and recruiter attention.

On the candidate side, the full system achieves a Gini of 0.4950 with
only 3.9\% of candidates starved and 82.9\% in the safe zone —
improvements of 28.2\% and 21.8\% respectively over the
bilateral baseline. The addition of the candidate-side penalty over
universe~2 reduces the starved fraction further from 6.8\% to 3.9\%
and brings the congested fraction down marginally from 13.7\% to
13.2\%, confirming that the candidate penalty is doing its job but
cannot fully counteract the structural effect of small inboxes created
by the job penalty. The remaining congestion is an inherent tension in
the dual-sided design: the mechanisms that most effectively reduce
candidate starvation also create pathways for mild candidate congestion
that the candidate penalty can only partially suppress.

\begin{table}[htbp]
    \centering
    \caption{Hybrid simulation results — job-side congestion on Window~7.
    Historical Reality shows the raw application distribution for
    reference. Universes~1 and~3 are identical on the job side by design
    since neither applies a job-side penalty; the same holds for
    universes~2 and~4.}
    \label{tab:hybrid_job_results}
    \begin{tabular}{@{}lcccc@{}}
        \toprule
        \textbf{Universe} & \textbf{Gini} & \textbf{Starved} &
        \textbf{Safe} & \textbf{Congested} \\
        \midrule
        Historical Reality          & 0.8575 & 80.9\% & 15.8\% & 3.3\% \\
        \midrule
        1: Bilateral (no penalties) & 0.9182 & 85.6\% & 11.1\% & 3.4\% \\
        2: Job-side sigmoid         & 0.7276 & 67.4\% & 29.4\% & 3.2\% \\
        3: Candidate-side sigmoid   & 0.9182 & 85.6\% & 11.1\% & 3.4\% \\
        4: Both sigmoid             & 0.7276 & 67.4\% & 29.4\% & 3.2\% \\
        \bottomrule
    \end{tabular}
\end{table}
\begin{table}[htbp]
    \centering
    \caption{Average match quality scores over all applied (user, job)
    pairs in Phase~1. Scores are determined by Phase~1 only and are
    therefore identical across universes that share the same job-side
    strategy.}
    \label{tab:hybrid_scores}
    \begin{tabular}{@{}lcc@{}}
        \toprule
        \textbf{Universe} & $\bar{p}_{\text{TT}}$ & $\bar{p}_{\text{rec}}$ \\
        \midrule
        1: Bilateral (no penalties) & 0.8966 & 0.8405 \\
        2: Job-side sigmoid         & 0.8671 & 0.8158 \\
        3: Candidate-side sigmoid   & 0.8966 & 0.8405 \\
        4: Both sigmoid             & 0.8671 & 0.8158 \\
        \bottomrule
    \end{tabular}
\end{table}
\begin{table}[htbp]
    \centering
    \caption{Hybrid simulation results — candidate-side congestion on
    Window~7. $\bar{p}_{\text{TT}}$ and $\bar{p}_{\text{rec}}$ are
    the average Two-Tower and recruiter probabilities of applied
    (user, job) pairs, serving as match quality proxies. Scores are
    only available for universes where Phase~1 differs.}
    \label{tab:hybrid_cand_results}
    \begin{tabular}{@{}lcccc@{}}
        \toprule
        \textbf{Universe} & \textbf{Gini} & \textbf{Starved} &
        \textbf{Safe} & \textbf{Congested}\\
        \midrule
        1: Bilateral (no penalties) & 0.6326 & 32.1\% & 61.1\% & 6.7\% \\
        2: Job-side sigmoid         & 0.5256 &  6.8\% & 79.5\% & 13.7\% \\
        3: Candidate-side sigmoid   & 0.5884 & 27.9\% & 66.2\% & 5.9\% \\
        4: Both sigmoid             & 0.4950 &  3.9\% & 82.9\% & 13.2\% \\
        \bottomrule
    \end{tabular}
\end{table}

Figure~\ref{fig:hybrid_job_grid} and Figure~\ref{fig:hybrid_cand_grid}
visualise the distributions across all four universes. The job grid
confirms the paired structure: bilateral and candidate-sigmoid are
visually identical, as are job-sigmoid and both-sigmoid. The contrast
between the two pairs is stark — the penalised universes show a sharp
drop in the tail beyond the congested threshold and a clear build-up of
mass in the safe zone. The candidate grid shows all four panels
diverging, with the progression from bilateral through to both-sigmoid
clearly visible in the shrinking starved spike at zero and one and the
extending but flattening tail.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{images/combined_job_counts_grid.png}
    \caption{Application distributions per job for all four hybrid
    universes on Window~7. Universes~1 and~3 (left column) are
    identical; universes~2 and~4 (right column) are identical. The
    note in the top-right of the left panels indicates jobs beyond the
    $x$-axis clip that exist due to the absence of a job-side penalty.}
    \label{fig:hybrid_job_grid}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{images/combined_candidate_views_grid.png}
    \caption{Shortlist distributions per candidate for all four hybrid
    universes on Window~7. All four panels differ, reflecting the
    independent contribution of each penalty. Universe~4 (both sigmoid)
    achieves the lowest starved fraction and best Gini at the cost of
    an elevated congested tail.}
    \label{fig:hybrid_cand_grid}
\end{figure}